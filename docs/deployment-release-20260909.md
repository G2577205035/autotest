# 2026-09-09 正式版本发布记录

## 当前发布：2026.09.11.2 录制窗口与验证指引

- 用户于 2026-09-11 明确授权更新服务器。发布前 399 项测试、194 项子测试通过；6 个录制相关源码文件及录制启动脚本变化，无数据库结构或依赖版本变更。
- Web、普通 Worker、UI Worker 与录制镜像更新；测试代理、网络、主密钥、外部依赖及五个卷保持原配置。三个新镜像各复用上一正式版本，离线构建；应用/UI Worker 各 118 个文件与本机 SHA-256 一致。
- 正式浏览器验证 1888×805 桌面、两窗左右等高、原始大小/适应/全屏、缺少验证指引、补加可见性验证、保存 v1、回放 1/1 通过，以及 7 个产物下载。目标仅打开蓝鲨登录页，无账号登录或业务数据写入；临时套件已删除，验收记录保留。
- 发布目录 `/home/liema/releases/2026.09.11.2`；镜像、源码及包哈希以该目录的 `RELEASE.json`、`image-ids.json`、`package-verification.json` 为准。回退至 `2026.09.11.1` 的环境与 Compose 副本位于 `/home/liema/rollback/2026.09.11.2`，旧镜像和上一版本加密 SQL 快照保留。
- 本机 `deliverables/liema-auto-2026.09.11.2-*` 只保留轻量发布/校验记录；完整离线包留在服务器。用户刷新平台并新建录制即可试用。


## 历史发布：2026.09.11.1 页面录制与回放

- 用户明确授权完成 Playwright 并部署。正式 Web、普通 Worker 保留原端口、主密钥、外部 MySQL/MinIO/Redis 与五个持久卷，新增 UI Worker、Internal 浏览器网络和显式允许列表代理；SSO/Vault、独立 Server Worker 未启用。
- 官方 Playwright 1.63.0 + noVNC 真实录制蓝鲨登录/任务管理及两个检查点，保存 v1；正式环境连续两次回放成功，第二次约 15.94 秒。7 个产物经 MinIO 授权下载验证，示例套件保留在蓝鲨项目。
- 398 项、194 项子测试通过；118 个安装文件哈希一致。实测发现并修复大核心主机 Chromium 线程耗尽、空 baseURL 无法创建浏览器上下文，以及浏览器退出后页面假录制状态。重连、取消、无检查点门禁、90 秒回收、普通脚本导入执行通过。
- 平台库 45 表加密 SQL 快照在独立库恢复并验证候选初始化记录数一致；旧环境/Compose 和镜像留存。没有新增全量 MinIO/卷快照，未修改业务原始库、服务器 Docker 或其他业务容器。
- 发布目录 `/home/liema/releases/2026.09.11.1`，镜像 ID 以 `image-ids.json`、`RELEASE.json` 为准；恢复资料位于 `/home/liema/rollback/2026.09.11.1`。使用与部署见 [UI 服务器部署手册](ui-server-deployment.md)。

## 历史发布：2026.09.10.2 测试集详情与进度排版

- 发布时间：2026-09-10；镜像 `liema-auto:2026.09.10.2`，静态 `20260910.2`；镜像 ID `sha256:d9794ee1c3e9f7d43b3055c57728071ee5c7b63acbafc582bf2ca847db7f2c7c`。
- 进度标签单行，待执行状态中文化；测试集标题 17px、正文/元信息 14px，桌面两列、窄屏单列；新增右侧详情、完整素材/参考答案/评分规则、每页 5 条的服务端分页、空草稿及错误重试。
- 309 项回归通过，末次 JavaScript 行为 4 项及结构/部署 5 项通过；1904/1366/390、双主题、内置集/项目集/草稿与长文本实屏通过。91 个安装文件哈希一致，断网 EvalScope 标准评测及性能 PoC 通过。
- 发布目录 `/home/liema/releases/2026.09.10.2`，前一镜像与目录保留；任务及 SSH 会话空闲后切换，Web/Worker 健康，既有端口、主密钥、依赖、五个持久卷保持一致。按用户要求不新增备份。
- 完整离线包保存在该目录，文件 `liema-auto-2026.09.10.2-release.zip` 与同名 `.sha256`；本地只保留轻量校验记录，构建暂存文件用后清理。报告和原模型实测内容沿用前次发布，不重复调用真实模型。

## 前次发布：2026.09.10.1 规范化评测报告与 AI 综合分析

- 发布时间：2026-09-10；镜像 `liema-auto:2026.09.10.1`，静态资源 `20260910.1`。
- 镜像 ID：`sha256:21aa983dec88ea44b9723eceffe3410370ce8e2a968eeb1bc2f50ace149ba77c`。
- 发布目录 `/home/liema/releases/2026.09.10.1`；当前指针与镜像配置同步，Web/Worker 健康，原端口、主密钥、外部依赖及五个持久卷保留。新增可选 `LIEMA_MODEL_CA_BUNDLE` 默认为空，不更改系统信任库。
- 报告采用正式章节，补齐环境、范围与版本、方法/判定标准、准确性控制、适用性及复验条件。保留全部 21 张指标表及 248 条用例；历史缺失环境明确标注，新任务自动采集执行端版本及只读硬件快照。
- 支持复用启用模型或单次选择已有模型生成 AI 综合分析；内置提示词版本 `model-assessment-20260910.4`，保留证据引用与缓存校验。普通查看/下载不调用模型。原始评分和平台判定不能被 AI 覆写。
- 307 项、95 项子测试通过；91 个安装文件及断网标准评测/性能 PoC 通过。指定内网模型完成真实总结，原任务报告与证据包已发布；PDF 27 页、Word 渲染 48 页逐页检查，桌面/手机浅深主题与分页验证通过。
- 当前启用的外部 DeepSeek 接口受到环境网关拦截，保持配置；本次显式选择已配置内网 `qwen38-27b` 完成总结，不等同于切换全局启用模型。仅解释原测量结果，不重新制造一轮测试数据。
- 交付文件为版本目录内 `liema-auto-2026.09.10.1-release.zip` 及同名 `.sha256`，含成品镜像、源码、离线依赖、换机手册与验证摘要。CRC、各文件及包外 SHA-256 校验见版本目录 `package-verification.json`，最终值不递归写入源码自身。
- 按用户要求不新增备份。大型部署包仅保留服务器，本地仅保留报告与轻量校验记录，清理已使用的临时部署文件。

## 历史发布：2026.09.09.10 全量测评与指标总报告

- 发布时间：2026-09-10；镜像 `liema-auto:2026.09.09.10`，静态资源 `20260909.6`。
- 镜像 ID：`sha256:435c931b6f3fc312ada544a2a32af70b23f41cbb5935a782a099aa7f80ea4a65`。
- 发布目录 `/home/liema/releases/2026.09.09.10`，当前指针与生产环境配置同步，Web/Worker 健康、零重启；主密钥、端口、依赖配置和五个持久卷保持一致。
- 全量 300 项、89 项子测试通过；90 个安装文件 SHA-256 及断网 PoC 通过。10 个真实专项、248 条质量明细、1529 次性能请求、526 点采样；有效裁判 45/220，缺测有明确原因。实际 PDF 23 页、Word 42 页逐页检查。详见 [全量验收](model-full-acceptance-20260909.md)。
- `.8` 为本次真实组合任务的执行版本；`.9` 完成报告与附件修正，`.10` 修复裁判示例诱导改名及维度完整性校验，4 条新端到端样本全部返回完整维度。原始测量保留，不覆盖历史评分。
- 交付文件为版本目录下 `liema-auto-2026.09.09.10-release.zip` 及同名 `.sha256`；包内包含成品镜像、源码、两套离线依赖、换机手册和验收摘要，ZIP CRC 与内外 SHA-256 全部校验。最终外层校验值见版本目录及本机轻量清单，避免递归写入源码包自身。
- 按用户要求不新增备份，Redis 保留可选密码；本地不下载大型交付包，并清理用完的临时部署/渲染文件。


## 历史发布：2026.09.09.7 模型实测与报告修正

- 应用镜像：`liema-auto:2026.09.09.7`；静态资源 `20260909.4`。
- 最终镜像 ID：`sha256:48afd57bb9a9c5e3d7d21b958c93828a1b9ce7a10ae0cd35f667e21fa9f7a531`。
- 发布目录：`/home/liema/releases/2026.09.09.7`；`/home/liema/current`、`CURRENT_RELEASE` 和生产镜像配置均指向本版。
- `.4`～`.6` 为本轮校准及修复过程版本，历史镜像/评测记录保留；当前交付只采用上述 `.7` 最终镜像。

本版保留此前全站对齐、16 个分页区域与全局主题，补齐双向采样、类型化等价评分、流式结束检查、独立裁判格式、真实主机资源关联、受控容量/恢复阶段及完整 DOCX/PDF。性能明细按实际执行顺序显示，非流式不再伪报首字延迟。详细范围与真实模型限制见 [最终验收记录](model-real-acceptance-final-20260909.md)。

全量 **286 项、77 项子测试通过**；结构与部署资产 **5 项、37 项子测试通过**。最终安装的 87 个源码/静态文件 SHA-256 一致；无源码树挂载、断网容器内 EvalScope 标准/WMT/性能 PoC 完成。真实验收 11 组，包括 260 条原生质量用例、40 次独立裁判、8 条适配器烟测、1549 次成功性能请求、3 次故障后真实调用与 81 点资源采样；最终 PDF 29 页、Word 48 页逐页检查完成。

本轮用户明确暂缓备份，升级没有新建数据库、对象或卷备份。此前发布记录中的备份属于历史操作。本版未做不兼容数据结构迁移，保留原生产主密钥、外部 MySQL/MinIO/Redis 配置、入口端口与五个持久卷；环境文件仍为 `0600`。Redis 密码为空时免密、有值时认证，密码错误不回退；没有修改共享 Redis、业务原始库或模型部署。

交付文件：`/home/liema/releases/2026.09.09.7/liema-auto-2026.09.09.7-release.zip` 及同名 `.sha256`。ZIP 包含完整镜像、源码、平台/EvalScope 离线依赖、换机手册、SHA-256 清单及验收摘要。内层文件逐项 SHA-256 和 ZIP CRC 校验后交付；最终校验值以版本目录及本机 `deliverables/liema-auto-2026.09.09.7/` 轻量清单为准，避免把 ZIP 自身校验值写入自身源码导致递归变化。

本机不下载镜像或离线依赖大包；用完的源码归档、构建脚本与渲染临时文件按工作区绝对路径白名单清理，轻量原始响应、DOCX/PDF、评分和校验记录保留。具体清理量及最终健康检查保存在同目录 `cleanup-record.json`、`final-health.json`；迁往另一台机器从 [换机部署手册](pycharm-lan-deployment-guide.md) 开始。

## 历史发布：全局浅色/深色主题（2026.09.09.3）

- 版本：`liema-auto:2026.09.09.3`；静态资源 `20260909.3`。
- 镜像 ID：`sha256:ab0ee9633a224f2af9335fea3990476cf4cfb07cce98b8d61c7f03471df769ac`。
- 前一版本：`liema-auto:2026.09.09.2`；源码基线仍为 `82a8b573fceafb5410be4414476024affb98d4f7`，含本次工作区改动。

登录/初始化页与全局业务顶栏新增主题按钮，默认深色，选择按浏览器同源保存，刷新前恢复并同步其他标签页。全站组件接入语义颜色，保留原深色规则作为回退；浅色适配表格、状态、编辑器、日志、弹窗、抽屉与图表。已绘制 Canvas 在主题变化时重绘。未增加服务器配置或数据库变更。

全量 **263 项、59 项子测试通过**；主题行为、存储异常回退、颜色令牌完整性和浅色文字对比度均有回归。24 个页面/页签在 1920 宽双主题、1280/390 宽浅色检查，另检查 1366 环境弹窗/成员抽屉及登录页、390 登录/初始化双主题；浏览器使用合成数据，不提交真实业务任务。详见 [界面验收记录](ui-layout-pagination-audit-20260909.md)。

使用保留的离线依赖在服务器断网构建完整镜像，安装后的 85 个源码/静态文件与本机一致，隔离 EvalScope 依赖检查通过。切换备份目录为 `/home/liema/backups/20260909-release-2026.09.09.3`，保存 33 表平台库、五卷逐文件校验的完整快照和原配置。发布目录为 `/home/liema/releases/2026.09.09.3`，前一版本及备份保留用于回滚。

实际发布入口首页与认证状态 200，三个业务分页接口匿名访问 401；首页、主题脚本/样式与应用脚本/样式哈希均与本机一致。Web/Worker 零重启、近期日志无 Traceback/ERROR，MySQL/MinIO/Redis external 与 Worker 心跳正常，生产环境文件权限为 0600。`/home/liema/current` 和 `CURRENT_RELEASE` 已指向本版本。最终视觉复核补强进度条/状态圆点对比度及顶栏窄屏排列；正式交付镜像以前述最终 ID 为准。

交付文件为版本目录下 `liema-auto-2026.09.09.3-release.zip` 及同名 `.sha256`；包含完整镜像、源码、离线依赖和新版换机手册。校验值保存在外部 `.sha256`，本机 `deliverables/liema-auto-2026.09.09.3/` 仅保留轻量记录。本地用完的源码临时包、构建/传输脚本与本次静态验收辅助文件清理，不下载镜像或依赖大包。

## 前次界面版本：全站对齐与分页修复

- 当前版本：`liema-auto:2026.09.09.2`；静态资源 `20260909.2`。
- 镜像 ID：`sha256:02c9f0e70ee5fa709b040f5920fb63543fcc00b3aba37ac5aa3de5a496ae9f35`。
- 前一版本：`liema-auto:2026.09.09.1`，镜像和版本目录保留用于应用回滚。
- 源码基线仍为 `82a8b573fceafb5410be4414476024affb98d4f7`，包含完整版本修复及本次 UI/分页改动；没有数据库结构变更。

逐页检查 9 个模块、24 个页面/子页签和附加弹窗；修复左右面板不齐、关闭抽屉占位与窄屏溢出。新增 16 个分页区域，报告、性能任务与场景报告按项目进行数据库分页，评测结果可访问第 200 条之后的明细。清单与浏览器证据见 [全站验收记录](ui-layout-pagination-audit-20260909.md)。

全量 **260 项、52 项子测试通过**，结构/部署 **5 项、37 项子测试通过**；真实 MySQL 只读分页和四种屏宽验收完成。使用保留的固定依赖在服务器断网构建完整镜像；83 个安装文件、正式入口 JS/CSS 与本地 SHA-256 一致，隔离 EvalScope 1.11.1 及依赖完整。Web healthy、Worker 心跳在线、MySQL/MinIO/Redis external 正常，两容器零重启，近期日志无 Traceback/ERROR；首页和认证状态为 200，三类分页接口匿名访问为 401。

切换备份位于 `/home/liema/backups/20260909-release-2026.09.09.2`：33 表平台库停机逻辑备份、五卷完整归档及逐文件哈希、原生产配置与 Compose；对象备份继续保留同日完整版本备份。首次切换因 Docker 卷列表返回顺序导致校验误报，自动恢复上一版；改为按挂载路径比较后重新核验原快照、再次备份数据库并成功切换。实际端口、主密钥、依赖配置和五卷挂载均保持一致，没有修改业务原始库或其他业务容器。

当前服务器目录为 `/home/liema/releases/2026.09.09.2`；`/home/liema/current` 与 `CURRENT_RELEASE` 均指向该版本。交付包为此目录下 `liema-auto-2026.09.09.2-release.zip` 及同名 `.sha256`，包内包含完整镜像、源码、两类离线依赖、清单及更新后的 `DEPLOYMENT_GUIDE.md`。换机先从服务器取包并校验，按 [换机手册](pycharm-lan-deployment-guide.md) 操作。本地仅保留轻量发布记录，不保留镜像/依赖归档；交付最终哈希以外部 `.sha256` 和本地 `deliverables/liema-auto-2026.09.09.2/` 清单为准。

## 前次完整版本记录：2026.09.09.1

- 发布版本：`liema-auto:2026.09.09.1`，静态资源版本 `20260902.9`。
- 前一版本：`liema-auto:2026.08.24.3`，旧镜像与原部署配置保留。
- 最终镜像 ID：`sha256:6749dae64be80ba1eb455ab97c7e6684412c4e1b9cb2da8d4ca048fd49ef2aae`。
- 源码基线：`82a8b573fceafb5410be4414476024affb98d4f7`，包含本次安装包入口修复、回归测试及部署定义更新。

## 发布范围

同步当前平台全部功能，包括模型评测、用户权限工作台和阶段事件编码修复。新增 `deploy/docker/Dockerfile.evalscope-offline`，在已验证的平台镜像上安装隔离 EvalScope 环境。

EvalScope 固定为 1.11.1，使用 Python 3.12.14 独立虚拟环境和 126 个固定依赖。依赖包保留官方来源、许可证元数据和 SHA-256；仅以源码分发的依赖另外保留源包并构建 wheel。NLTK `punkt_tab` 资源锁定来源提交和哈希。安装使用实体 Python 文件，避免虚拟环境符号链接被路径解析到主服务解释器；子进程入口从实际安装包解析，部署目录无需源码树。

## 备份与升级

备份编号：`20260909-release-2026.09.09.1`。备份存放于正式部署目录的受限 `backups/` 中，生产环境文件副本保持 `0600`，未进入普通交付归档。

| 项目 | 已验证结果 |
| --- | --- |
| 平台数据库 | 33 张表，单事务压缩逻辑备份；停止平台服务后再次备份；gzip 与 SHA-256 校验通过 |
| 平台对象 | 607 个对象，54,032,914 字节；源清单稳定，SFTP 传输后逐对象 SHA-256 一致 |
| 持久卷 | 五个卷完整归档；27 个产物文件与 6 个暂存文件逐文件回读哈希一致，其余三个卷为空 |
| 原部署 | 原镜像 ID、源码、配置、端口及卷归属已记录，旧镜像保留 |

本次确认开发与正式环境使用不同的平台专用对象前缀，分别有 580 与 27 个对象；两处均纳入备份，保持正式服务原前缀不变。历史的对象枚举数量差异应先核对前缀，再判断客户端或网络问题。

停止服务前再次确认业务自动化、报告、接口场景、压测和模型评测五类任务没有排队/运行记录，活动 SSH 连接为 0。先备份，再依次更新 Worker 和 Web。保持原 Compose 项目、Web 端口、主密钥、MySQL/MinIO/Redis 配置与五个持久卷，没有修改业务原始数据库或其他业务容器。

## 验收证据

- 上级 `.venv` 全量：257 项通过、49 项子测试通过。
- 项目结构与部署资产：5 项通过、37 项子测试通过。
- 断网 Linux 容器：EvalScope 标准评测、WMT BLEU、性能压测均完成；通过无源码树的安装包子进程检查。
- 安装后的 83 个源码/静态文件与本机 SHA-256 一致；局域网实际返回的 JavaScript、CSS 与发布源码一致。
- 首页、认证状态返回 200；认证状态禁止缓存，管理员初始化状态保留；匿名业务接口返回 401。
- Web 为 healthy，Worker 心跳在线，MySQL/MinIO/Redis 与 external 模式正常；两个容器均零重启，近期日志无 Traceback/ERROR。

以上评测使用本机假模型服务，不代表真实模型质量或容量；本次未代用户登录或提交真实业务任务。真实模型维护窗口、Redis ACL/密码及不同故障域备份继续按 `PROJECT_PLAN.md` 推进。

## 交付与回滚

本机交付目录为 `deliverables/liema-auto-2026.09.09.1/`，包含源码包、平台依赖包、EvalScope 依赖包、镜像归档、清单与发布记录。以同目录 `SHA256SUMS` 为最终交付校验依据；源码与镜像不含生产环境文件、开发配置、平台数据或真实凭据。

2026-09-09 文档修订：交付 ZIP 根目录新增 `DEPLOYMENT_GUIDE.md`，配套文档放在包内 `docs/`，覆盖全新部署和旧平台迁移；生产变量模板同步当前镜像标签。`RELEASE.json` 以 `documentation_revision=2026-09-09.2` 标识本次修订。源码归档、文档和 ZIP 校验值随之更新，应用镜像、静态资源与功能测试基线保持一致，无需为此重启已部署服务。

服务器当前构建目录为 `/home/liema/releases/2026.09.09.1`，生产环境文件仍位于原部署目录。需要回滚时，先按部署手册确认无活动任务，再使用旧部署定义与明确旧版本依次恢复 Worker、Web，并重新验收健康和匿名门禁。数据库和对象结构没有在本次升级中做不兼容变更；不得为了应用镜像回退直接覆盖现网数据。

## 本地空间清理与再次取包

2026-09-09 按用户要求，在重新核验服务器 ZIP 后删除本机五个部署归档、离线构建依赖副本及本次发布临时文件，释放 2,566,819,577 字节；本机保留手册、清单、验收证据和 `local-cleanup.json`，不再保存可直接传输的部署大包。开发与 EvalScope 运行环境、配置、密钥及运行数据未清理。

再次换机时，通过受控 SFTP/SCP 从上述服务器版本目录取回 `liema-auto-2026.09.09.1-release.zip` 和同名 `.sha256` 文件，然后按部署手册先校验再解包。保留的 ZIP SHA-256 为 `1808c6a8362340820c488f4fe18f128300c288f94ce0effd096f7138c33076fe`；服务器上的交付包、源码、镜像和回滚备份均保留。

## 后续发布：2026.09.10.3 全站分页

当前更新镜像为 `liema-auto:2026.09.10.3`，镜像 ID 为 `sha256:72b65d68a09ac708247cd62561d110234514f14f184a2a850658b485afaf4239`，静态版本 `20260910.3`。统一 24 处固定列表与全部动态评测报告章节的跳页、每页条数和有界滚动；保留现有主题、报告与测试集详情能力。

314 项全量回归和 5 项结构/部署专项通过。真实 MySQL 24 组只读分页、正式入口静态哈希、匿名接口 401、91 个安装文件及断网 EvalScope 评测/性能 PoC 通过。切换前确认五类任务全部终态、SSH 会话为零；按用户要求不新增备份，生产环境变量与数据卷保持一致。

服务器版本目录为 `/home/liema/releases/2026.09.10.3`，交付 ZIP 与同名 `.sha256` 保留于该目录；ZIP 内 60 项 SHA-256 和 CRC 均已核验。本机 `deliverables/liema-auto-2026.09.10.3/` 保存轻量清单与验收证据，外部校验值以其中的 `.zip.sha256` 文件为准，不在文档重复固定。再次取包和换机按最新部署手册执行。

## 后续发布：2026.09.10.4 分析与布局修复

2026-09-10 用户明确授权部署。当前镜像 `liema-auto:2026.09.10.4`，镜像 ID `sha256:4a62efc08e5419f89cd41c751d4ef6f91b5926284f4dbb8a3e146b342b7920eb`，静态资源 `20260910.4`。修复模型接口/正文 JSON 解析与诊断、正文校验失败的一次纠正重试，以及双栏列表伸展和分页贴底；不改变实测数据与模型配置。

321 项发布前全量回归（58.405 秒）、5 项结构/部署回归通过；此前 51 项 UI 定向和多尺寸双主题实屏检查保留。91 个安装文件逐项哈希一致，断网 EvalScope 评测和性能 PoC 完成；正式入口四项 JS/CSS 哈希、五项匿名接口门禁、Web 健康及 Worker 心跳均通过。升级前五类任务全部终态，活动 SSH 会话为零；环境、端口及五个持久卷一致，容器零重启，按用户要求不新增备份。

交付目录 `/home/liema/releases/2026.09.10.4`；完整 ZIP、同名 `.sha256` 和换机手册由服务器保存，本机仅保留 `deliverables/liema-auto-2026.09.10.4/` 的轻量发布与校验文件。内外校验值以 `SHA256SUMS`、`package-verification.json` 和 `.zip.sha256` 为准。继承的真实模型验收与报告仍标明原测量/报告版本，本次没有新增真实模型调用。兼容回退版本为 `2026.09.10.3`，无需恢复数据库。
