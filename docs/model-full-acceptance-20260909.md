# 全量测评入口与真实指标验收

> 验收日期：2026-09-10；需求启动：2026-09-09。当前报告版本 `2026.09.10.1`；原始测量版本保持 `2026.09.09.8`。

模型评测新增“全量测评（全部维度）”按钮。默认执行所有内置质量和性能专项，也可附加当前项目完整的已发布素材测试集；停止/失败保留已完成数据。报告使用 21 张指标表和全部用例附录，支持页面分页、Word、PDF 和带 SHA-256 清单的证据 ZIP。

## 报告规范化复验（2026-09-10）

本次使用同一组原始实测数据重编报告，不覆盖历史响应、裁判或性能统计。增加总体适用性分析、测试环境、方法与判定依据、可信度限制及证据索引；新任务在开始时采集软件/硬件环境，旧任务不能以当前硬件补造历史快照。

已有配置中的 `qwen38-27b` 已实际生成证据约束的 AI 综合分析，提示词版本 `model-assessment-20260910.4`。结论为“具备受控验证依据，尚不具备生产放行条件”，分场景解释能力证据、裁判覆盖缺口、人工复核与业务 SLA 要求。总结模型与独立裁判用途分离，不改写分数；生成模型可单次选择，不改变全局启用配置。启用的 DeepSeek 因网络网关返回 HTML 尚不可用，本次明确采用内网模型。

报告 PDF 27 页、Word 渲染 48 页已全部检查，原 21 张指标表及 248 条明细完整。原任务的 MinIO 报告及证据 ZIP 已同步，ZIP 增加分析输入、内置提示词、生成结果和逐文件 SHA-256。307 项、95 项子测试通过；最终 19 项报告/结构/部署复验通过；实际服务器只读环境采集、安装文件校验和断网 PoC 均完成。

## 本次真实执行

任务 `0ddcafef2ff04b7f92c31c42e0eb3baa`，状态 `completed`。被测模型为用户指定配置实际保存的 `qwen38-27b`，独立裁判为 `Qwen3-4B`，资源采样绑定实际模型主机的已保存资产；另加入此前验收使用的 4 条项目业务素材。

| 专项 | 计划样本 | 实际执行 | 状态 | 异常原因 |
| --- | ---: | ---: | --- | --- |
| 基础能力与鲁棒性（非流式） | 10 | 10 | 已执行 | — |
| 基础能力与鲁棒性（流式） | 10 | 10 | 已执行 | — |
| 中英双向翻译 | 200 | 200 | 已执行 | — |
| 标准 Benchmark | 4 | 4 | 已执行 | — |
| WMT2024++ 标准翻译 | 4 | 4 | 已执行 | — |
| 报告写作能力 | 8 | 8 | 已执行 | — |
| 情报生产能力 | 8 | 8 | 已执行 | — |
| 项目自定义测试集 | 4 | 4 | 已执行 | — |
| 并发阶梯 | 受控负载 | 320 | 已执行 | — |
| 深度性能与容量 | 受控负载 | 1209 | 已执行 | — |

质量明细共 **248 条**，其中原生质量 **240 条**，达标 **239**，未达标 **1**，调用异常 **0**；适配器调用成功与质量达标分开。有效主观裁判 **45/220**，人工已复核数量以报告为准，设置抽检比例不会自动算作人工验收。

全部 **12 个性能阶段**共 **1529 次主测请求**，失败 **0 次**；预热不混入总数。资源采样 **526 点**，实际对齐 **12 个性能阶段**。质量预算 4096 Tokens / 120 秒，性能最多 512 Tokens / 16 并发，保留各阶段目标 RPS、真实执行时间窗和请求预算。

## 报告可信度与限制

报告分别列出原生规则、每项 rubric、ROUGE-L 召回和 BLEU-1 原始分数；每个性能负载都有请求数、成功/失败数、实际 RPS、端到端与 TTFT 的 P50/P95/P99/平均值、输入输出用量及吞吐。四类服务器资源保留采样数、最小/平均/峰值、阶段对齐与相关性。来源版本、输入、参考、响应、裁判理由及最新人工复核通过证据包追溯。

本轮 220 条主观样本中，45 条具备完整有效维度；174 条翻译的裁判把“忠实度与完整性”改成了提示示例的“事实忠实度”，另 1 条裁判输出达到 4096 Tokens 上限而截断。维度原样匹配，缺项不会补成满分或零分；219 次返回 completed 不等于 219 次完整评分。每项有效样本量单独列出。

1 条情报用例规则未达标：要求“时间线”“证据映射”“矛盾与不确定性”，模型改写成“事件时间线”“证据映射与因果推断”“矛盾点与不确定性”。原规则使用明确标题匹配，故未达标不能直接解读为相关内容完全缺失；独立裁判给出高分也不能覆盖规则失败。原文还包含自行推断的报告时间、严重等级及因果归因，不能用高分证明事实完全正确。

内置问答/WMT 各 4 条是适配器烟测，翻译包含模板变体；此次结果不代表官方公开榜单、最大上下文认证或所有行业能力。负载回落不等于模型重启/故障注入恢复。

正式运行结果由 `.8` 测量引擎采集，`.9` 修正指标名称/单位、历史阶段顺序、附录状态和汇总附件选择；最终 `.10` 使用按实际维度生成的裁判模板，并拒绝缺项、改名、重复、非法数值，阻止无效裁判进入融合评分。另用当前被测模型和裁判对双向翻译、报告、情报 4 条样本完成新的端到端回归，4/4 裁判维度完整；这次小样本回归不冒充重跑历史全量，原始测量数据与历史评分均保留。详情及证据在平台当前项目的该全量任务中可下载。

## 验证与交付

- 全量 **300 项测试、89 项子测试通过**；包含完整组合、停止/失败、1003 条项目集、报告完整分页、缺测、有效裁判边界和项目隔离。
- 90 个安装源码/静态文件 SHA-256 一致，断网容器内已安装包的 EvalScope 标准/WMT/性能 PoC 通过；换机手册同样的只读文件系统命令已实际执行。
- 390/1280/1745 CSS 宽页面检查，深浅主题与独立表分页通过；本地隔离夹具只用于界面检查，不作为真实模型结果。
- 实际报告 PDF **23 页**、Word **42 页**逐页渲染检查通过；完整表头、页码、全部用例、21 组指标核验完成。证据 ZIP 的 **35 个成员**逐文件哈希通过。
- 最终版本在服务器保留完整离线镜像、源码、两套依赖及换机手册。本地只保留代码、文档、报告和轻量验收证据；用完的部署归档与临时渲染文件按用户要求清理。
- 本次不新增备份，不更改共享模型/GPU 服务、数据库业务原始数据或 Redis 认证策略。备份及多系统/GPU 矩阵、UI 自动化继续按用户安排暂缓。

轻量证据目录：`deliverables/qwen-full-acceptance-20260909/`；发布清单：`deliverables/liema-auto-2026.09.09.10/`。换机步骤见 [部署手册](pycharm-lan-deployment-guide.md)。
