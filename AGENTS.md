# Workspace guidance

开始任何开发前，先完整阅读根目录的 `PROJECT_PLAN.md`，再阅读 `PROJECT_STRUCTURE.md`。前者是烈马自动化测试平台的唯一规划、进度和下一步入口，后者是目录职责和接手导航；仅在任务相关时再读取 `docs/` 专题或扫描源码。

工作约束：

- 完成实质改动后同步更新 `PROJECT_PLAN.md` 的日期、进度、当前工作和测试数量。
- 任何新增、删除、移动或重命名目录、源码分层、启动入口、部署文件的改动，都必须在同一次改动中同步更新 `PROJECT_STRUCTURE.md`，并运行项目结构回归测试。
- 业务服务器与 GPU 服务器默认同机；独立 GPU 服务器只能由用户明确指定。
- 不得硬编码公司服务器，不得修改业务原始数据库，不得在文档或代码中新增明文凭据。
- 不要擅自占用本机 8080 端口；用户通过 PyCharm 启动服务。
- 保留现有业务自动化能力，按模块边界渐进改造，不推倒重写。
