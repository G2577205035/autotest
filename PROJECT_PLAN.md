# 烈马自动化测试平台：项目规划与当前进度

> 最后更新：2026-09-01  
> 用途：新会话先读本文件，再决定是否查看源码或历史工作日文档。  
> 状态标记：`[x]` 已完成，`[~]` 进行中/部分完成，`[ ]` 未开始，`[!]` 需要外部资源。

## 1. 三十秒项目上下文

- 平台名称：**烈马自动化测试平台**。
- Python 项目名：`auto-test`；源码包：`src/auto_test/`。
- 技术栈：FastAPI、原生 HTML/CSS/JavaScript、MySQL、MinIO、Redis、Paramiko、Matplotlib、DOCX/PDF 报告。
- 当前运行方式：PyCharm 启动 `web_main.py`；不要擅自占用或保留本机 `8080` 端口。
- 当前自动化测试：**223 项**；已在 PyCharm 实际使用的上级 `.venv` 完成全量回归，**223 项全部通过**，另有 42 项子测试通过。
- 当前存储：项目环境已启用 MySQL 与 MinIO 适配器；只使用平台专用存储空间，严禁修改业务原始数据库。
- 当前本机配置与正式服务器均使用 `redis + external`；正式服务器已按用户授权临时复用受控内网的免密 Redis，使用独立 `liema-auto-production` 命名空间且未修改现有 Redis 配置。Web/Worker 已通过 Compose 上线，长期运行仍需补齐 Redis ACL/密码认证。
- 磁盘最外层目录仍保留旧名称，仅为当前 PyCharm/Codex 工作区路径；Python 包、命令和页面名称已经完成改名。关闭项目后可单独重命名外层目录。

## 2. 不可违背的产品与安全原则

1. 不把任何公司服务器写成“默认服务器”；服务器由用户在页面选择或输入。
2. 业务服务器与 GPU 服务器**默认是同一台**，复用同一 SSH 连接和凭据；只有用户明确填写不同地址时才拆成两台。
3. `nvidia-smi` 监控与 `gpu-burn` 压测解耦。只要驱动和 `nvidia-smi` 正常，即使无法安装 `gpu-burn` 也必须展示 GPU 指标。
4. 服务器在线体检、业务场景监控和专业 CPU/GPU 压测是不同能力，不能混为一个结论。
5. 平台不自动升级驱动、CUDA、内核或容器运行时，也不自动停止业务容器。高风险操作必须明确确认并记录审计。
6. GPU 正在承载业务时允许在线监控；满载压测默认受安全策略阻止。`gpu-burn -m` 控制显存使用比例，不等于精确控制 GPU 计算利用率。
7. DCGM 是可选诊断增强，不是 GPU 压测或基础监控的强制依赖。
8. 页面面向普通测试用户：优先使用业务名称、清晰状态和图表，不能直接展示难懂的对象、接口结构或仅提供 TXT。
9. 报告必须面向阅读者，至少支持 DOCX/PDF、关键图表、结果摘要、异常说明和结论。
10. 任何文档、日志和提交中都不得新增密码、Token、SSH 密钥或数据库密钥。

## 3. 原始产品规划（压缩版）

### 3.1 产品模块

- **现有业务自动化**：保留上传、解析、翻译、断言、摘要、AI 检查和导出流程，但封装为产品适配模块，避免绑死整个平台。
- **接口管理与接口自动化**：按项目、模块、接口、用例、场景管理；支持接口串联、变量提取、断言、数据驱动、批量执行、轻量并发和接口报告。
- **服务器性能测试**：独立于业务自动化；先通过服务器资产与长连接会话进行实时监控，再在同一会话内执行只读预检、CPU/GPU 压测、安全保护和独立报告。
- **模型配置中心**：统一管理模型地址、模型名、密钥和启用状态，密钥加密保存。
- **模型评测中心**：基于内置标准测试集和项目自定义素材，评估 Token/上下文、指令遵循、翻译、鲁棒、并发负载、报告写作和情报生产能力；质量与性能分维度展示并生成独立报告。
- **报告中心**：业务自动化、接口自动化、服务器性能分别生成适合各自场景的报告。
- **用户与权限**：接入正式用户登录，支持用户、项目、角色、权限和审计；临时页面 Key 门禁已取消。
- **UI 自动化（后续）**：基于 Playwright，先实现脚本导入、管理、运行和结果查看，再评估浏览器内录制。

### 3.2 目标架构

- 当前采用“可拆分的模块化单体”，不推倒重写。
- MySQL 保存用户、项目、任务、接口版本、报告元数据和审计记录。
- MinIO 保存输入快照、日志、图表、DOCX/PDF 和压测产物。
- Redis 后续用于任务队列、Worker 心跳、短期状态和实时事件，不作为最终数据源。
- 中期拆分 Web、Automation Worker、Server Worker、Report Worker。
- SQLite 与本地文件继续作为开发/单机应急模式。

### 3.3 UI 自动化可行性结论

技术上可行，但不直接做复杂在线录制：

1. 第一阶段：导入 Playwright 脚本、套件管理、参数/环境配置、隔离运行、截图/录像/trace 和报告。
2. 第二阶段：提供受控的步骤编辑器与常用动作，不承诺自动修复所有选择器。
3. 第三阶段：评估短生命周期浏览器容器 + noVNC 的网页内录制。

主要风险是浏览器资源隔离、并发、账号/凭据、选择器稳定性、录制会话安全和非 Web 控件兼容性。完成接口自动化 MVP 和 Worker 隔离后再启动该模块。

## 4. 当前能力进度

| 模块 | 状态 | 当前结果 | 下一缺口 |
| --- | --- | --- | --- |
| 项目命名与品牌 | [x] | 页面、API、报告、Python 包统一为烈马/`auto_test`，Logo 已接入 | 外层工作区目录待关闭项目后重命名 |
| 工程结构与接手指引 | [x] | 根目录已收敛为项目入口与治理文件；部署、文档工具和历史资料已分区；`PROJECT_STRUCTURE.md` 统一说明目录职责、代码层级和变更规则，并由结构回归守卫 | 后续任何层级变更必须与指引同次更新 |
| 架构边界 | [x] | Task Repository、Platform Store、Artifact Store、Task Signal Queue 已抽象；Redis 仅承载小型唤醒信号，数据库保持最终事实源 | Secret Provider 企业实现、独立 Server Worker |
| MySQL / MinIO | [x] | 适配器、迁移、核验、恢复和下载链路已实现；2026-08-24 已完成平台专用库 25 表、MinIO 539 对象和 5 个持久卷的同机隔离恢复演练 | 离机/异地备份副本、保留策略和定期恢复计划 |
| 持久化任务 | [x] | 自动化、接口场景、压测、报告具备状态持久化；业务账号密码、SSH 密码和可选管理员授权进入独立加密凭据表，执行器领取时销毁且不进入任务历史；Redis 外部模式下业务自动化、接口场景和报告由独立 Worker 领取，队列中断可退化为数据库轮询 | 压测/SSH 会话迁入独立 Server Worker、跨实例租约和外部 Secret Provider |
| 现有业务自动化 | [x] | 上传、解析、翻译、断言、AI 检查、导出和实时日志保留；业务账号直接按任务提交，服务器可选择加密资产或手动输入，不再依赖 YAML 账号/服务器模板；服务器直传支持大文件自适应校验超时、传输/校验进度和失败任务正确收口；翻译平均速度按持久化进度事件的实际增量与时间戳实时统计 | 进一步做产品适配层，减少接口变更影响 |
| 企业报告 | [x] | DOCX/PDF、不可变快照、图表、规则/模型结论、MinIO 下载；翻译吞吐量同步进入快照及 DOCX/PDF 结果章节；Linux 图表从实际 Noto CJK 字体文件注册，历史任务生成报告时重绘性能图并排除无字体版本标记的旧乱码图；报告候选使用轻量运行目录标记，列表不暴露完整路径且能正确识别已产生产物的任务；生成、模板、任务三个工作区已全部满宽 | 页面进一步简化报告配置 |
| 模型配置 | [x] | 页面配置、连通测试、启用、密钥加密；连通测试具备卡片内进行中/成功/失败反馈，耗时请求在线程池执行，不再冻结 Web 响应 | 项目级权限和调用审计 |
| 模型评测 | [~] | 已完成 V1.2 技术方案；采用“EvalScope 标准评测/性能引擎 + 平台业务编排与专项评分”，保留内置与用户测试集、翻译/鲁棒/报告/情报维度、运行快照、安全护栏和企业报告；开发排期已压缩为 4 个工作日 | 工作日 1 完成固定版本与隔离运行 PoC，工作日 2～4 连续完成基础 MVP、业务专项、深度性能和交付回归 |
| 接口中心 | [x] | 已完成项目级模块、接口、环境、普通变量与加密密钥引用的可视化管理；接口目录可直接进入上下分区的请求调试工作台，支持完整 http(s) URL 或环境相对路径、Query、请求头、JSON/multipart/form-urlencoded/原始文本请求体、变量替换、超时控制与即时响应详情；请求地址栏可直接粘贴 cURL 并自动拆解回填；兼容 JSON/YAML/OpenAPI/cURL 导入、不可变版本、发布、RBAC 与审计；接口资产可进一步编排为可执行场景 | 后续补充更丰富的数据驱动和历史趋势 |
| 服务器会话管理 | [x] | 服务器资产列表、配置/密码独立保存、主密钥加密凭据、持续 SSH/指标采集、心跳、显式停止、5 分钟空闲回收及会话内压测复用均已实现；活动 SSH 会话带当前项目归属，同一目标不会跨项目复用，列表、详情、心跳、指标、停止和压测提交均隐藏其他项目会话；CPU 温度只采纳 lm-sensors 当前 Package/Tctl/Tdie/Core 值或可识别的 CPU sysfs 热区，并保留来源与传感器名称；实时监控与压测配置页可直接切换活动会话且状态同步 | 跨 Web 实例共享会话需随独立 Server Worker/Redis 落地 |
| 服务器兼容性 | [x] | OS/架构/权限/工具/磁盘/内存/容器/GPU 只读预检及降级结论 | Ubuntu、CentOS 和更多 GPU 型号实机矩阵 |
| CPU 压测 | [x] | `stress-ng` 优先，Python 降级；线程、负载、时长和阈值支持；安全保护温度已排除 sensors 的 high/crit 阈值和非 CPU 热区 | 标准化基准分数与跨机器对比 |
| GPU 压测 | [~] | 逐卡选择、繁忙保护、源码/容器执行器、CUDA 11.8/12.8 镜像策略、显存比例 | 4090/5090/A6000/PRO 6000 等实机验收 |
| GPU 实时监控 | [x] | 总体概览、逐卡卡片/曲线、当前/平均/峰值齐全；支持显存容量/占比、功耗/上限、频率、风扇、P-State 及 available/partial/unavailable 状态；多卡宽屏使用四列状态卡，默认只展示使用率、显存占用和温度，使用状态色细边框与圆点表达健康度，完整指标和曲线按需整行展开 | 更多 GPU 型号与驱动版本实机矩阵 |
| 服务器性能报告 | [x] | 独立 DOCX/PDF 已同步增强 CPU 温度来源、GPU 指标、逐卡表格/曲线和采集状态；任务记录可选择任一报告并支持逐行下载 | 增加跨任务对比和历史趋势 |
| 前端体验与设计系统 | [x] | 建立 1920×1080 可读性基线并保持业务工作台统一深色；平台品牌与模块标题已统一为 18px / 17px 的清晰层级，所有模块共用同一顶栏规则；日常项目选择器、接口目录和项目卡片只显示业务项目名称，内部项目标识只在管理/编辑场景出现；登录入口根据实屏反馈改为铺满窗口的深浅 58/42 分屏；接口中心改为项目/模块目录与接口清单同屏管理，新建接口使用接近专业 API 工具的信息层级，空目录提供用途说明和主操作入口；服务器实时监控按“会话状态—压测任务—总体指标—逐卡详情”组织信息；业务任务实时监控已增加可信 CPU 封装温度当前值/峰值和翻译平均速度卡，双路服务器逐 Package 展示，翻译速度明确给出统计增量、观察时长与当前进度，阶段进度按时间正序排列并自动跟随底部最新节点，日志与指标轮询按事件 ID 去重并阻止同任务并发刷新；运行总览已去除演示式准备度评分，历史任务改为案件、目标环境、测试内容、耗时和结果等真实业务信息；服务器资产继续使用可筛选表格与右侧抽屉；压测配置折叠状态不再被实时轮询覆盖，并可在页内直接切换活动会话；实时会话选择、状态与关闭操作使用统一控件基线 | 后续模块继续复用扁平信息层级，并以实际浏览器截图验收尺寸、对齐和留白 |
| 用户管理 / RBAC | [x] | 首位管理员安全初始化、账号密码登录、HttpOnly 会话、CSRF、用户启停、平台/项目角色、项目成员关系、审计记录均已落地；登录失败按 IP+用户名执行 5 分钟/5 次限流，认证响应禁止缓存，改密立即撤销旧会话；平台品牌与业务项目概念已分离，新建项目自动切换，平台管理员可通过顶栏切换；业务任务、报告、上传、活动 SSH 会话和压测按当前项目隔离，无项目标记的旧产物仅在初始项目向平台管理员兼容展示；审计记录使用每页 20 条的数据库分页 | 后续接入企业 SSO 和外部 Secret Provider |
| 接口自动化 | [x] | 已完成场景持久化与可视化步骤编排、用例/环境/提取变量递归替换、JSON/响应头/正文/状态码/耗时取值、12 类断言、失败停止或继续、最多 20 场景与 1～5 并发批量执行、持久化后台排队/停止/重启恢复、结果历史及 DOCX/PDF 报告；运行结果和审计统一脱敏并按项目/RBAC 隔离 | 数据集驱动、定时执行和跨运行趋势留待后续迭代 |
| UI 自动化 | [ ] | 已完成方案论证，未编码 | Playwright Runner PoC |
| 部署体系 | [~] | 正式服务器已在专用目录完成 Web/Worker 部署，复用既有 MySQL、MinIO、Redis 且没有创建第二套依赖服务；生产环境文件权限为 `0600`，Web 健康、Worker 心跳、匿名门禁和局域网入口均通过；外网软件源异常时使用官方依赖、逐文件 SHA-256 校验与 `--network=none` 完成离线镜像构建；Linux 图表使用 Noto CJK，PDF 在 CFF 字体不受支持时使用中文 CID 字体；2026-08-24 完成备份、隔离恢复、运行态快照回滚和固定镜像再前滚；2026-08-28 新增从 PyCharm 构建、交付并部署到新局域网的完整操作手册 | 将临时免密 Redis 迁移到 ACL/密码认证；建立不同故障域的备份副本 |

## 5. 十个工作日计划与当前标记

这是根据原始十日计划和当前代码重新校准后的唯一排期基线。

| 工作日 | 内容 | 状态 |
| ---: | --- | --- |
| 1 | 架构审计、模块边界、服务器兼容策略 | [x] |
| 2 | Repository/ArtifactStore、压测调度与安全停止 | [x] |
| 3 | MySQL/MinIO 适配、迁移、核验与回滚 | [x] |
| 4 | 服务器预检、GPU 执行器、真实 openEuler/RTX 3090 验证 | [x] |
| 5 | 服务器性能页面、GPU 同机监控、实时指标和报告闭环 | [x] RTX 4090 增强字段实机复验完成 |
| 6 | 用户登录、用户管理、角色和项目级 RBAC MVP | [x] |
| 7 | 接口资产编辑器：项目/模块/接口/环境/变量/密钥引用 | [x] |
| 8 | 接口自动化场景：串联、提取、断言、批量执行和报告 MVP | [x] |
| 9 | Redis 队列、独立 Worker、Docker Compose 与部署/升级方案 | [~] 正式服务器部署、局域网验收、备份恢复与镜像回滚演练已完成，待 Redis 认证加固和离机备份副本 |
| 10 | 多系统/多 GPU 回归、权限安全检查、交付文档和验收 | [~] 权限、会话、项目隔离、脱敏安全检查、交付文档、正式部署及灾备演练已完成，待外部实机矩阵 |

## 6. 当前正在做与下一步

### 根目录与接手指引整理（已完成）

- [x] 根目录只保留 `main.py`、`web_main.py`、`pyproject.toml`、README、规划/结构指引和工具配置，并清理可重建的根目录 Python/pytest 缓存；原先散落的 Compose、Dockerfile 与生产变量模板统一迁入 `deploy/`，一次性方案生成器迁入 `scripts/documentation/`，旧日报模板和截图迁入 `docs/archive/`、`docs/assets/`。
- [x] 新增 `PROJECT_STRUCTURE.md`，完整登记一级目录、`src/auto_test/` 模块边界、部署层级、运行数据流、接手顺序与层级变更检查清单；`AGENTS.md` 和 README 已明确要求目录、模块、入口或部署文件变化时必须在同一次改动中更新该指引。
- [x] Compose 在迁移后继续使用项目根目录作为构建上下文，在线/离线镜像、正式部署手册、GPU 兼容策略、交付清单和测试路径已同步更新；补齐 README 原有的 `docs/pycharm-lan-deployment-guide.md` 断链。
- [x] 新增项目结构守卫，防止根目录重新出现未登记文件，并检查源码与部署目录已进入指引；路径引用扫描、Python 语法和部署定向回归通过。PyCharm 实际使用的上级 `.venv` 全量 **223 项全部通过，另有 42 项子测试通过**。

### 模型评测模块（方案已完成，4 个工作日开发待启动）

- [x] 完成 `docs/model-evaluation-technical-design.md`，明确模型评测中心独立于模型配置，复用现有 Store、Artifact Storage、Redis 信号队列、外部 Worker、服务器指标、RBAC 和报告基础设施。
- [x] 确定基础能力、Token/上下文、单请求性能、并发负载、资源可靠性、鲁棒性、翻译、报告写作和情报生产指标；翻译作为一级维度，首版内置中英双向素材并支持用户术语表和双语语料。
- [x] 确定“内置只读标准集 + 项目自定义测试集 + 不可变版本”的素材体系，以及规则、独立裁判模型、人工复核的分层评分体系；能力质量与服务性能不强制合成一个总分。
- [x] 复核 EvalScope 官方仓库、标准/自定义数据集、Mock、裁判、逐请求性能、`perf` 并发/RPS/开放环、WMT2024++、服务模式、扩展接口和 Apache-2.0 许可；方案更新为 `EvaluationBackend` 隔离适配，不复用 EvalScope WebUI，不把长同步服务接口直接作为平台任务系统。
- [x] 明确第三方数据集许可证与 EvalScope 代码许可证分开管理；标准数据集和 COMET/BERTScore/Tokenizer 等附加模型需在受控有网环境预取、锁版本和哈希，正式环境强制离线加载。
- [x] 用户已确认将原四阶段开发压缩为 **4 个工作日**，每个工作日完成一个可验收阶段；不删减隔离运行、完全离线、协作停止、项目权限、凭据保护和回归要求。
- [ ] 当前下一步（工作日 1）：完成 EvalScope 候选固定版本在 Python 3.11 下的能力、`perf`、WMT、Mock、停止、结果结构和完全离线兼容性 PoC，同时落地 `EvaluationBackend`、数据表、Store 契约、结果映射、Mock 与 Worker 骨架。
- [ ] 随后（工作日 2）：完成 Token/上下文、首批标准 Benchmark、并发阶梯、鲁棒性、基础 API、运行监控和结果明细 MVP。
- [ ] 收口（工作日 3～4）：工作日 3 完成翻译、报告写作、情报生产、裁判/人工复核和 DOCX/PDF；工作日 4 完成深度性能、资源关联、模型对比、全量回归与交付。真实模型或维护窗口未及时提供时，对应实机项标记为外部待验，不阻塞代码交付。
- [x] 本次仅调整技术方案和排期，未修改运行代码；自动化测试基线保持 **223 项全部通过，另有 42 项子测试通过**，本次未重复执行代码测试。

### 工作日 10：权限安全检查与交付验收（进行中）

- [x] 完整复核登录门禁、HttpOnly 会话、CSRF、角色权限映射、项目选择和公开路径；报告生成与服务器操作分别使用 `report:manage`、`server:operate`，不再依赖含义过宽的默认执行权限。
- [x] 登录接口新增按“客户端 IP + 规范化用户名”的 5 分钟/5 次失败限流，第 6 次返回 429 与 `Retry-After`；失败和限流均写入不含密码的审计记录，认证状态、初始化、登录和退出响应统一使用 `Cache-Control: no-store`。
- [x] 用户密码改变时立即删除该用户全部服务端会话，避免旧设备或泄漏令牌在改密后继续有效；账号停用继续立即撤销会话，最后一位平台管理员保护保持不变。
- [x] 修复旧产物越权入口：时间戳格式的无项目归属报告、图表和目录枚举只允许平台管理员访问；现代任务 ID 继续按当前 `X-Project-ID` 校验，越权统一返回 404。
- [x] 浏览器暂存上传新增独立项目归属元数据，提交任务时必须属于当前项目；无归属的历史上传仅平台管理员可兼容使用，归属文件存放在上传树外，不会被带入业务上传内容。
- [x] 活动 SSH 会话新增项目归属：同一服务器只在同一项目内复用，列表、详情、心跳、指标、停止和基于会话创建压测任务均校验当前项目；无归属旧会话仅平台管理员可见。
- [x] 补充旧产物、上传、服务器会话、权限映射、登录限流、认证禁止缓存和改密撤销会话回归；测试 Web 使用独立 SQLite 路径和本地产物存储，不再依赖运行中的 MySQL/MinIO。Python 编译通过，PyCharm 实际使用的上级 `.venv` 全量 **207 项全部通过**；当前在线 `/health` 同时确认 MySQL、MinIO、Redis 与独立 Worker 正常。
- [x] 用户重启 PyCharm Web 后完成运行态烟雾复验：`/health` 返回 200，MySQL、MinIO、Redis、external 模式与独立 Worker 心跳正常；匿名 `/api/auth/status` 返回 `Cache-Control: no-store`，匿名业务接口返回 401；已登录的用户/项目、固定角色矩阵、审计记录和项目化服务器会话工作区均正常加载，当前项目为“蓝鲨”、活动会话为 0，页面无横向溢出或控制台错误。当前正式库只有一个项目，未为制造越权样本而新增账号/项目；CSRF 拒绝与跨项目 404 继续由隔离回归覆盖。静态页面未修改，资源版本保持 `20260821.1`。
- [x] 完成交付与验收说明 `docs/delivery-acceptance.md`，统一记录交付范围、角色权限、功能验收、安全边界、217 项测试基线、已知限制和验收记录模板；完成 `docs/deployment-backup-rollback-runbook.md`，覆盖服务器部署前置信息、Redis 生产认证边界、MySQL/MinIO/持久卷备份、升级、恢复和回滚步骤。README 已增加统一入口；文档不含公司服务器、数据库名或明文凭据。
- [x] 正式服务器部署完成：只创建平台专用目录、Compose 网络、Web/Worker 和 5 个平台卷；没有停止、删除或修改任何既有目录、容器以及 MySQL/MinIO/Redis 配置。生产环境文件权限为 `0600`；共享免密 Redis 按用户授权临时复用，使用独立 `liema-auto-production` 命名空间，当前仅有带 TTL 的 Worker 心跳键。镜像使用官方 Python 基础镜像、官方离线依赖和 SHA-256 清单构建，构建阶段强制无网络；Web 容器健康且零重启，Worker 在线，首页/认证状态为 200、匿名业务接口为 401，工作站经局域网访问 `/health` 为 200，返回 MySQL、MinIO、Redis、external 模式和 Worker 在线。首次启动暴露的镜像配置文件与空卷属主问题已在不删除、不重建现有容器的前提下原地修复，并固化到 `deploy/docker/Dockerfile.offline` 回归测试。
- [x] 部署相关新增 Redis 环境命名空间、离线镜像构建、生产文件排除、Linux Noto CJK/中文 CID 字体回归；审计记录增加同秒内按自增 ID 稳定倒序，消除登录限流测试的偶发顺序问题。正式服务器按产品长期受控局域网 HTTP 使用方式设置 `LIEMA_SESSION_COOKIE_SECURE=false`，解决账号验证成功但浏览器拒绝 Secure Cookie 而无法保持登录的问题；Cookie 继续保留 HttpOnly、SameSite 和 12 小时服务端会话，未来切换 HTTPS 可显式改回 `true`。Web 已原地更新并健康，Python 实际使用的上级 `.venv` 全量 **214 项通过、4 项子测试通过**。
- [x] 项目切换体验与隔离收口：平台管理员创建项目后自动切入新项目并返回运行总览；切换时清空旧任务选择、监控游标和页面缓存。正式库确认新项目误见内容来自无项目标记的历史兼容数据，现仅在最早创建的初始项目展示，现代数据继续严格按当前项目隔离。用户审计接口增加总数、页码、每页大小和数据库 `LIMIT/OFFSET`，页面默认每页 20 条并提供上一页/下一页、当前范围和总数；静态资源提升到 `20260822.1`，全量 **217 项通过、4 项子测试通过**。正式服务器已原地更新同一个 Web 容器并保持健康，未删除或重建任何目录和容器；真实 MySQL 核验初始项目可见 33 条运行、新项目可见 0 条，1196 条审计的第二页返回 20 条；同版本离线镜像已重建供后续受控升级使用。
- [x] 修复正式 Worker 首次进入图表执行链时缺失 `packaging` 的离线镜像缺陷：根因为构建器已安装该依赖，带 `--prefix=/install` 的应用安装误判依赖已满足，最终镜像未复制该包。现将 `packaging` 声明为直接运行时/离线依赖，并通过 `--ignore-installed` 强制把全部运行时依赖写入最终复制目录；Web/Worker 已原地安装缺失包，仅在确认三类任务队列均无排队或运行任务后重启 Worker，未删除或重建任何现有目录和容器。执行链导入、局域网 `/health`、MySQL、MinIO、Redis 和 Worker 心跳均正常；离线镜像已重新构建，日志确认最终安装集合包含 `packaging 26.3`。失败任务因一次性凭据在领取时已销毁，保留原失败记录并由用户通过“重新配置”安全重提。Python 编译通过，全量 **218 项通过、4 项子测试通过**。
- [x] 按客户操作语义修正“服务器直传”数据来源：删除要求填写平台服务器/容器本地绝对路径的字段，自动、平台直传和服务器直传三种模式均从当前客户电脑选择测试文件夹；服务器直传先形成项目隔离的共享暂存快照，再由 Worker 通过 SSH/SFTP 送达业务服务器。Web 提交接口拒绝任何绕过浏览器上传的平台本地路径引用，避免容器路径暴露和越权读取；重新配置历史任务也不再回填旧后台路径。静态资源提升到 `20260822.2`；隔离浏览器确认服务器直传模式下本地目录选择器保持启用、后台路径字段不存在、传输说明正确，Node 语法、Python 编译及全量 **218 项通过、4 项子测试通过**。
- [x] 完成正式服务器平台专用数据灾备与镜像回滚演练，编号 `20260824-164909`：无活动任务窗口内生成 MySQL 压缩逻辑备份、MinIO 539 对象逐对象 SHA-256 清单和 5 个持久卷快照；新建验证库恢复 25 张表并核对 8 类关键计数，唯一 MinIO 验证前缀回读 539 个对象及 DOCX/PDF 文件签名，5 个临时卷逐文件哈希一致，全部临时目标已清理。发现服务器 `mc` 和生产容器路径只能枚举 18 个对象，未误判成功，改由完整枚举客户端通过 SFTP 加密通道落盘并把“三方清单一致”固化到手册。Web/Worker 运行态快照、固定镜像前滚、基线回滚和再次前滚均通过健康、Worker、匿名门禁及依赖导入检查；最终容器统一到固定镜像，最近日志无错误。全量 **218 项通过、4 项子测试通过**，Node 语法和 Python 编译通过；实际记录见 `docs/disaster-recovery-rehearsal-20260824.md`。
- [x] 修复报告中心始终显示“暂无可生成报告的任务”：轻量任务列表原本省略 `run_dir`，前端却仍按该字段筛选，导致所有候选被误删。列表现仅返回 `has_run_dir` 布尔标记，既不暴露完整产物路径，又能正确筛出报告来源；前端保留旧响应兼容并将静态资源版本提升到 `20260824.1`。正式 MySQL 只读核验 35 条任务可识别 26 条报告候选且未泄露路径；Node 语法、Python 编译和全量 **219 项测试全部通过**。正式服务器已在四类任务队列均为空的窗口离线构建并切换到新镜像，Web 健康、Worker 在线、容器内报告候选为 26 条、最近日志无错误；旧镜像和变更编号 `20260824-180649` 的代码/环境备份均保留用于回滚。
- [x] 修复正式报告图表中文显示为方框：原逻辑只把“微软雅黑”名称写入 Matplotlib 配置却不验证字体是否存在，Linux 最终回退到不含中文字形的 DejaVu Sans。现从镜像内 Noto CJK 字体文件直接注册，并复用于业务自动化和服务器性能报告；历史运行在生成新报告前会从原始性能 CSV 重绘图表，无当前字体版本标记的旧统计图不再混入报告。正式镜像已升级到 `2026.08.24.3`，容器实测使用 `Noto Sans CJK JP` 且缺字警告为 0，Web 健康、Worker 在线、最近日志无错误，回滚备份编号 `20260824-191914`。已为最新历史任务生成新的 DOCX/PDF，PDF 10 页经 Poppler 全量渲染和逐页视觉检查，中文标题、图例、坐标轴均正常；Python 编译和全量 **222 项测试全部通过**。
- [x] 新增 `docs/pycharm-lan-deployment-guide.md`，完整覆盖 Windows/PyCharm 构建 Linux 镜像、在线/离线依赖路径、SHA-256 交付校验、外部 MySQL/MinIO/Redis 最小权限准备、生产变量逐项填写、跨主机传输、首次启动、局域网访问、健康/功能验收、升级、回滚和常见故障；明确容器不可使用 `127.0.0.1` 访问宿主机依赖、MinIO bucket 必须预建、HTTP/HTTPS Cookie 边界和稳定主密钥要求。README 已增加统一入口；部署资产定向 **4 项测试通过**，全量自动化基线保持 **222 项通过、4 项子测试通过**。

下一步仅保留三项：1）协调现有 Redis 的 ACL/密码认证并在维护窗口把临时免密连接切换为专用凭据；2）为演练备份配置不同故障域的离机/异地副本、保留策略和定期恢复计划，并继续排查服务器侧 MinIO 枚举差异；3）取得 Ubuntu、额外 GPU 型号或明确释放的目标卡后，补齐外部实机矩阵和正式满载验收。

### 工作日 9：Redis 队列、独立 Worker 与页面响应优化（进行中）

- [x] 完成本地慢请求只读诊断：本轮开始时项目使用远程 MySQL 与 MinIO，本机没有 Redis Python 包、Redis 服务、`redis-cli` 或 Docker；MySQL 首次建连约 **295.8 ms**，连接池预热后工作区查询约 **1.3～2.3 ms/次**。因此普通页面慢的主要风险是远程存储冷连接、登录后一次性加载过多模块，以及耗时任务与 Web 共进程争用；缺少 Redis 本身不是普通查询变慢的直接原因。
- [x] 新增可插拔 Task Signal Queue：默认 `local + embedded` 保持 PyCharm 单进程直接可用；`redis + external` 使用 Redis List 发送小型唤醒信号，任务正文、状态和结果始终保存在 SQLite/MySQL。Redis 连接异常时提交不失败、任务不丢失，Worker 自动退化为数据库轮询。
- [x] 新增独立 `auto-test-worker` / `python -m auto_test.worker` 入口，承载业务自动化、接口场景和企业报告；Redis 心跳带 20 秒 TTL，`/health` 返回队列可用性、执行模式和 Worker 在线状态。服务器 SSH 长连接与压测仍留在单 Web 进程，避免在未建立跨进程会话所有权前冒进拆分。
- [x] 接口场景执行从长时间 HTTP 请求改为持久化后台任务：提交立即返回 `202 queued`，Worker 原子领取，页面短轮询状态；支持排队中立即停止、运行中协作停止、异常终态兜底、服务重启恢复、批次持久化并发限制（1～5）和报告生成。旧 SQLite/MySQL 表自动增加快照、参数、停止标志、并发上限及领取索引，旧 `NULL` 快照记录也能安全恢复或中断。
- [x] 登录后的前端加载由固定请求运行、报告、模型、接口、模板、压测、身份等 7 个模块，收敛为首屏运行/报告 2 个请求；模型、接口、服务器、身份与报告模板只在进入对应视图后加载。接口场景排队、执行、停止和 Worker 未连接提示均已接入页面，静态资源版本提升到 `20260820.2`。
- [x] 新增通用 Dockerfile、Redis/Web/Worker `deploy/compose.yml`、持久卷、服务健康检查和脱敏环境变量占位；README 补充 PyCharm 单进程、Redis 外部模式、Worker 启动、镜像版本化升级及回滚边界。当前电脑没有 Docker，已完成 YAML 结构校验但未冒充完成容器实跑。
- [x] Redis 客户端现支持 `SERVICE_REDIS_IP/PORT/PASSWORD` 与 `LIEMA_REDIS_HOST/PORT/USERNAME/PASSWORD` 拆分注入，密码作为独立客户端参数传入，不进入 URL、YAML 或日志；已在 PyCharm 实际使用的上级 `.venv` 安装 `redis 8.1.0`，Web 模块导入、Redis/Worker 状态及队列 6 项回归均通过。目标实服 TCP、匿名 `PING`、随机隔离命名空间的唤醒/阻塞领取/心跳均通过，单轮约 **16.7 ms**，探针键已清理。
- [x] 本机 `config.local.yml` 已正式切换为 `redis + external`；按用户要求仅在本机专用配置中填写 Redis 主机、端口和 DB，未把公司地址写入示例配置或业务代码，密码仍只通过 `password_env: SERVICE_REDIS_PASSWORD` 从运行环境注入，不保存明文。
- [x] 经用户明确授权，目标 Redis 暂按免密方式完成隔离联调：两个独立客户端分别模拟 Web 与 Worker，使用随机命名空间和临时 SQLite 验证持久化排队、跨客户端唤醒/领取、排队中停止与 Worker 心跳，全部通过；一次完整领取约 **108.1 ms**，探针键和临时数据库均已清理，未接触真实任务。
- [x] 正式 Worker 启动前只读确认真实数据库中业务自动化、企业报告和接口场景待领取任务均为 0；随后用进程环境注入免密 Redis 地址启动真实 Worker，配置解析为 `redis + external`，Redis/Worker 均在线且持续观察无调度错误，8080 保持未占用。
- [x] 2026-08-21 由用户从 PyCharm 启动 Web 后完成真实 `/health` 核验：MySQL、MinIO、Redis、`external` 执行模式和独立 Worker 心跳全部正常；启动前再次只读确认业务自动化、企业报告和接口场景待领取任务均为 0。
- [x] 新建只读本机 `/health` 接口资产与单步验收场景，避免访问现有业务接口；首次提交暴露 MySQL 严格模式兼容缺陷：运行记录未显式写入三个非空报告路径字段而在入队前返回 500。现已显式初始化 `artifact_dir`、`docx_path`、`pdf_path`，新增回归并经真实 MySQL、Redis 与独立 Worker 复验通过。
- [x] Web 端到端闭环最终约 **1.38 秒**完成，单步 HTTP 200 断言通过，场景执行耗时 **23.0 ms**；MinIO 中 DOCX **39,219 字节**、PDF **55,059 字节**均可实际读取且文件签名正确。结果详情中 `passed` 步骤曾误显示“尚未执行”，现已统一显示“通过”，静态资源版本提升到 `20260821.1`。
- [x] 登录后首屏及懒加载实测完成：首屏数据可见就绪约 **3.44 秒**；模型配置、接口中心、服务器性能、用户与项目分别约 **313 / 326 / 314 / 325 ms**，浏览器控制台无警告或错误。该数据为真实浏览器从操作到内容可见的就绪时延，不冒充底层网络分段耗时。
- [~] 目标 Redis 当前无需认证即可取得 `default` 身份。经用户明确授权，正式服务器暂以独立 `liema-auto-production` 命名空间复用，启动前前缀为空、启动后仅存在带 TTL 的 Worker 心跳键；平台不会在“配置了密码但认证失败”时静默退回匿名连接，长期运行仍需 Redis 管理员启用密码或提供正确 ACL 用户名。
- [x] Python 编译、Node JavaScript 语法、Compose YAML 校验以及队列、接口场景、SQLite/MySQL 迁移/连接池、静态页面等回归通过。新增 MySQL 严格模式报告路径回归后测试总量更新为 **198 项**，PyCharm 实际使用的上级 `.venv` 已完成全量 **198 项全部通过**。

该阶段的本机队列、Worker 和 Web 闭环已完成，工作已进入并完成工作日 10 的权限安全、运行态复验和交付文档部分；剩余事项以本节顶部的两项当前下一步为准。

### 工作日 8：接口自动化场景 MVP（已完成）

- [x] SQLite/MySQL 新增项目隔离的接口场景与执行记录；场景保存时校验环境和接口资产归属，最多支持 50 个启用步骤，并阻止删除仍被场景引用的接口资产或环境。
- [x] 接口中心新增“场景自动化”工作区：支持场景名称/说明/默认环境、普通用例参数 JSON、可视化步骤增删排序、步骤环境覆盖、失败后继续、变量提取和响应断言；场景列表支持多选批量执行，执行历史可查看完整步骤交换并下载报告。
- [x] 场景执行按步骤串联接口请求，环境、场景参数与前序提取变量可递归替换后续 URL、Query、Header、Body 和断言期望值；支持 JSONPath 子集、响应头、正文正则、状态码和耗时提取，支持等于/不等于/包含/存在/数值比较/正则/区间等 12 类断言，未配置断言时默认校验 HTTP 200～399。
- [x] 单场景和批量执行均持久化结果；批次最多 20 个场景、并发度限制为 1～5，请求执行并行而 DOCX/PDF 生成在进程内串行保护。必需提取或断言失败默认停止后续步骤，也可按步骤选择继续。
- [x] 敏感用例参数在发送请求前拒绝，环境密钥和动态 Token 仅在运行内存中解密/串联；请求快照、响应、提取值、断言、审计和报告统一脱敏，不持久化明文密钥。场景管理使用 `interface:manage`，执行使用 `test:execute`，查询与下载遵循项目可见权限。
- [x] 新增面向阅读者的 DOCX/PDF 接口报告，包含执行结论、通过/失败/跳过统计、逐步骤请求/响应摘要、提取与断言结果；使用中文与长文本混合的 6 步样本逐页渲染检查，PDF 3 页、DOCX 5 页均无裁切，7 张 DOCX 表格的宽度、缩进、列网格和单元格宽度结构审计全部一致。
- [x] 页面静态资源版本提升到 `20260820.1`；Node JavaScript 语法、Python 编译、接口定向 49 项及当前环境可加载的全量 **173 项测试全部通过**。测试总量更新为 **188 项**；其余 15 项仍需在带 `httpx2` 的 PyCharm 项目环境复跑。

该阶段已完成；接口场景现已在工作日 9 改为持久化后台队列执行。

### 任务调度稳定性与存储性能加固（已完成）

- [x] 定位挂机任务夜间故障根因：本机空闲 5 小时自动睡眠（S3）冻结进程并掐断 SSH/MySQL 连接，唤醒瞬间网卡未就绪导致 `WinError 10065` 无路由与 `10054` 连接复位；睡眠停滞被计入翻译平均速度观察时长，叠加错误刷屏，造成“越跑越慢”的观感。服务器部署无睡眠问题，但网络抖动会触发同类故障。
- [x] MySQL 存储引入有界连接池（默认 12 条、5 秒获取超时）替代“每操作新建连接”：空闲连接复用、异常连接丢弃、池满快速失败；`MySQLConnection.execute` 对 2006/2013/2055 断连错误自动重连重放一次；`connect_timeout` 默认收紧为 5 秒，两份配置改为 3 秒。
- [x] 心跳陈旧不再立即判死：`runs` 新增 `stale_marked_at`，首次超时仅进入 120 秒宽限期并记录 WARNING 事件，宽限结束仍未恢复才中断；心跳成功立即清除标记并恢复消息。SQLite 与 MySQL 均带旧库自动迁移（`ALTER TABLE` 增列 + 条件建索引）。
- [x] 翻译进度事件结构化：`run_events` 新增 `event_type` 列与 `(run_id, event_type, id)` 索引，`翻译进度/翻译完成` 事件写入时即打标，统计查询走索引并保留旧数据 `LIKE` 回退，替代随日志量线性变慢的全扫描；`list_runs` 列表查询不再拉取 `run_dir/error` 大字段，状态接口改为按需取全量。
- [x] 任务日志写库降级：`_RunLogHandler` 写库失败不再抛 `handleError` 错误风暴，改为内存缓冲（上限 2000）+ 指数退避（最高 60 秒）+ 批量补写，健康告警只走不落库的控制台 logger。
- [x] Web 层同步数据库调用移入线程池：`/api/runs`、`/api/runs/{id}`、logs、status、metrics 及任务提交/报告创建等热点端点不再在事件循环内阻塞，MySQL 抖动时事件循环保持响应；`/api/runs/{id}` 的翻译速度事件读取也移入线程池。
- [x] 新增回归：心跳宽限期与恢复清标、旧库自动迁移、事件类型查询与旧行回退、列表瘦身、日志降级缓冲补写、连接池复用/满载快速失败/坏连接替换、断连重试一次与非连接错误不重试、MySQL 条件增列/建索引；定向 39 项通过。全量 182 项中 164 项通过，剩余 13 项因本会话网络无法访问目标内网 MySQL/MinIO（连接复位）未执行，属环境因素，PyCharm 环境需复跑确认。
- [x] 本次未修改页面资源，静态版本保持不变。

当时默认下一步为工作日 8；该项现已完成，当前进入工作日 9。

### 接口工作台 cURL 粘贴快速填充（已完成）

- [x] 请求地址栏监听 cURL 粘贴，识别后自动回填 HTTP 方法、去除 Query 后的完整 URL、Query 参数、请求头、超时与请求体；接口名称为空时根据 URL 末级路径补全，并自动切换到最相关的参数分页。
- [x] 后端只做结构化解析，不执行粘贴命令；兼容 `curl` / `curl.exe`、POSIX 反斜杠、Windows `^` 和 PowerShell反引号多行续写，支持 JSON、multipart/form-data、x-www-form-urlencoded、原始文本和 `-G` Query 语义。
- [x] `@file`、`--upload-file`、Cookie 文件等客户端本地文件引用会明确拒绝，不读取运行平台文件；`Content-Length`、multipart boundary、`--location` 和 `--insecure` 按平台安全策略处理并给出可见提示。
- [x] 单接口保存与即时调试同步持久化 `body_type`；服务器发送 multipart 时重新生成正确 boundary，旧接口未声明类型时继续按 JSON 兼容。页面静态资源版本为 `20260819.9`。
- [x] 新增用户截图原样 `--form`、JSON、URL Query、`curl.exe`、本地文件拦截、结构化导入和实际请求编码回归；Node JavaScript 语法、Python 编译及接口相关 43 项通过。测试总量更新为 **169 项**，当前环境可加载的 **154 项全部通过**，其余 15 项仍需 `httpx2`。

当时默认下一步为工作日 8；该项现已完成，当前进入工作日 9。

### 任务详情翻译进度查询 MySQL 百分号转义修复（已完成）

- [x] 定位 `GET /api/runs/{id}` 500 根因：`get_translation_progress_events` 使用 `message LIKE '翻译进度%'` 过滤，MySQL 适配器把 qmark 占位符转成 `%s` 后交给 PyMySQL 的百分号格式化，两个 LIKE 通配 `%` 被当作格式占位符，单参数对三个占位符抛出 `TypeError: not enough arguments for format string`；SQLite 直连路径不受影响。
- [x] `_placeholders` 在转换占位符时同步把 SQL 字面 `%` 转义为 `%%`，PyMySQL 格式化后恢复为单个 `%`，LIKE 语义不变；这是适配层通用修复，覆盖今后所有经 MySQL 执行的含百分号 SQL。
- [x] 新增 MySQL 适配器百分号转义与单参数格式化两项回归；全量 **162 项** 执行 161 项通过、4 项子测试通过，唯一失败为 `test_desktop_typography_has_readable_1080p_baseline_and_cache_version` 仍断言旧静态版本 `20260819.8`（页面已是 `20260819.9`），已同步断言并单独复跑该文件 31 项通过。
- [x] 本次未修改页面资源，静态版本保持 `20260819.9`。

当时默认下一步为工作日 8；该项现已完成，当前进入工作日 9。

### 业务任务翻译平均速度与报告同步（已完成）

- [x] 新增统一的翻译吞吐量统计：只解析持久化的“翻译进度/翻译完成”事件，以相邻进度点的实际完成增量除以实际时间戳间隔；停滞时间会计入观察时长，总数变化或进度回退会自动切分独立轮次，避免多账号、多批次跨空档误算。
- [x] 后端直接读取任务的完整翻译进度事件，不依赖前端仅保留的最近 500 条日志；任务详情接口返回平均速度、统计增量、有效观察时长、采样点、轮次和当前/总进度，实时监控顶部新增第五张“翻译平均速度”状态卡。
- [x] 企业报告不可变快照升级到 `1.1`，DOCX/PDF 结果章节新增“翻译吞吐量”表，页面与报告共用同一个后端统计结果；静态资源版本提升到 `20260819.8`。
- [x] 用户截图示例按精确时间 `14:31:04 → 14:34:16` 核验为 `27 / 192 × 60 = 8.44 个/分钟`；覆盖停滞、进度重置、噪声过滤、完整事件查询、报告快照/DOCX 和页面接线回归。Node JavaScript 语法、Python 编译及定向 48 项通过；测试总量更新为 **160 项**，当前命令行可执行的 **145 项通过**，其余 15 项仍需 `httpx2`。

当时默认下一步为工作日 8；该项现已完成，当前进入工作日 9。

### Apidog 风格单接口调试工作台（已完成）

- [x] 参考 Apidog 的核心请求工作流，将接口目录与编辑器从弹窗改为同一工作区：目录点击“调试”后，上半区集中维护方法、URL、Query、请求头、JSON 请求体、环境与超时，下半区固定展示请求结果；未照搬尚未实现的场景、报告等导航空壳。
- [x] 新增项目隔离的 `POST /api/interface-debug` 执行入口，支持完整 URL、环境 Base URL + 相对路径、项目/环境变量递归替换、1～120 秒超时、禁止自动重定向和 2 MB 响应预览；连接失败、超时和变量缺失返回可读错误。
- [x] 响应区展示 HTTP 状态、耗时、大小、格式化响应体、响应头和实际请求；Authorization、Cookie、Token、API Key 与 Set-Cookie 等敏感头统一脱敏，审计只记录方法、目标主机/path、状态码和耗时，不记录查询参数、请求体或密钥。
- [x] 调试权限使用 `test:execute`，接口保存、发布和删除继续使用 `interface:manage`；只读成员不会获得发送入口。静态资源版本提升到 `20260819.7`。
- [x] 1920×1080 隔离浏览器实屏完成“接口目录 → 调试工作台 → 真实 HTTP 发送 → 响应体/响应头/实际请求”闭环验收；Node 语法、Python 编译及可用环境 **137 项测试通过**。测试总量更新为 **152 项**；15 项 FastAPI TestClient 集成测试因当前命令行环境缺少 `httpx2`，需在 PyCharm 项目环境复跑。

### 接口新建 400 与工作台可读性修复（已完成）

- [x] 定位 `POST /api/interface-assets` 400 根因：手工新建接口仍沿用旧运行时覆盖校验，强制请求地址必须以 `/` 开头，导致合法完整 URL 被拒绝；现支持 `http://`、`https://` 完整 URL 和绑定环境后的 `/path` 相对路径，并拒绝账号密码、Fragment、上级路径与非 HTTP 协议。
- [x] 通用接口入口彻底移除“替换原 path”，普通新建、编辑与批量导入均不再提交旧蓝鲨运行时覆盖字段；底层字段只保留历史数据兼容，不再污染独立接口测试产品入口。
- [x] 新建接口改为宽屏 HTTP 请求编辑器：方法与地址同一主轴，接口名称/模块/环境集中在元数据区，Query、请求头、JSON 请求体和说明独立分页；JSON 或后端校验失败在窗口内就地显示，不再只弹瞬时提示。
- [x] 接口清单增加 HTTP 方法颜色、独立完整地址拼接逻辑和结构化空状态，页面明确说明接口中心不依赖业务自动化任务；静态资源版本提升到 `20260819.6`。
- [x] 1920×1080 浏览器实屏验收接口目录与新建窗口，实际完成完整 URL、POST、请求头和 JSON 请求体录入/切换；使用隔离临时数据库实调 `POST /api/interface-assets`，用户示例完整 URL 已由原 400 变为 201 草稿且 `default_path` 为空。Node 语法、Python 编译、地址安全校验与静态页面定向 32 项通过，可用环境其余回归合计 **134 项通过**。测试总量更新为 **148 项**；14 项 FastAPI 集成测试因当前命令行环境缺少 `httpx2`，需在 PyCharm 项目环境复跑。

### 业务项目名称与内部标识显示边界修复（已完成）

- [x] 顶栏“业务项目”和接口中心“当前业务项目”选择器统一只显示项目名称，例如“蓝鲨”，不再重复显示 `LIEMA · 蓝鲨`。
- [x] 接口目录项目卡片删除内部项目标识，名称下方固定说明其为“业务项目”；项目标识仍保留在首次初始化、创建项目和管理员编辑项目的管理场景中。
- [x] 静态资源版本提升到 `20260819.5`；1920×1080 实屏确认三个日常位置均只显示“蓝鲨”、无 `LIEMA`、无横向溢出，Node JavaScript 语法及 PyCharm 虚拟环境全量 **144 项测试通过**。

### 服务器直传大文件超时、无进度与假成功修复（已完成）

- [x] 根据 6,921,179,102 字节 RAR 和 31 秒失败间隔定位根因：复用同日服务器目录后，对同名同大小文件执行远端 MD5，却沿用 SSH 命令默认 30 秒超时；`TimeoutError` 的空消息又导致日志只剩“上传失败”。
- [x] 远端 MD5 改为按文件大小计算 300～3600 秒超时，本次 6.45 GiB 文件可用 826 秒；远端校验每 30 秒报告等待状态，本机 MD5 与 SFTP 传输按 5% 或 15 秒节流输出进度，完成后再次核对服务器文件大小并始终关闭 SFTP。
- [x] 上传异常统一保留异常类型和可读原因；任何账号上传失败都会让任务进入失败状态，不再显示“自动化测试执行完成”，并停止监控、保存部分性能样本、释放所有独立 SSH 连接。
- [x] 实时日志和指标按持久化事件 ID 合并去重，同一任务的刷新增加互斥保护；静态资源版本提升到 `20260819.4`，浏览器确认新版脚本加载且无控制台错误。
- [x] 新增大文件动态超时、已有文件跳传、失败传播/资源释放和前端轮询回归；Node JavaScript 语法、Python 编译、定向 31 项及 PyCharm 虚拟环境全量 **143 项测试通过**。

### 平台品牌与模块标题层级收敛（已完成）

- [x] 桌面端平台品牌统一为 18px、模块标题统一为 17px、模块说明统一为 13px，所有模块共用顶栏规则，平台名称重新成为更高一级的视觉主标题。
- [x] 品牌 Logo 收敛为 44px 并调整品牌区间距；1920×1080 实屏确认平台名称完整显示，侧栏品牌区和顶栏均无横向溢出。
- [x] 静态资源版本提升到 `20260819.3`；Node JavaScript 语法、静态页面定向 27 项及 PyCharm 虚拟环境全量 **139 项测试通过**。

### 业务任务阶段进度顺序与自动跟随（已完成）

- [x] 阶段进度改为时间正序：最早节点在上、最新节点在下，阅读方向与实时日志一致；仍只保留最近 14 个阶段节点，避免列表无限增长。
- [x] 每次收到新阶段后自动滚动到容器底部，最新节点始终可见；用户可向上滚动查看较早阶段。
- [x] 静态资源版本提升到 `20260819.2`；浏览器使用超长阶段列表确认存在滚动空间时 `scrollTop` 等于最大滚动位置，最后一个节点位于底部可视区域。
- [x] Node JavaScript 语法、静态页面定向 26 项及 PyCharm 虚拟环境全量 **138 项测试通过**。

### 业务任务实时监控补充 CPU 封装温度（已完成）

- [x] 在业务任务实时监控顶部新增“CPU 封装温度 当前 / 峰值”卡片，与 CPU 使用率、GPU 和内存指标同屏展示；温度统计覆盖前置压测到后续业务执行的整段任务采样。
- [x] 复用可信 CPU 温度口径，只接受 `lm-sensors` 的 Package/Tctl/Tdie/Core 当前值或可识别的 CPU sysfs 热区；双路服务器分别展示 `Package id 0`、`Package id 1`，不采纳 high/crit 阈值和非 CPU 热区。
- [x] 使用当次真实任务的 156 条可信采样核验双路 Package 数据；静态资源版本提升到 `20260819.1`，1920×1080 四列布局和 1280×720 无横向溢出实屏验收通过。
- [x] Node JavaScript 语法、温度/监控定向 35 项及 PyCharm 虚拟环境全量 **137 项测试通过**。

当时默认下一步为工作日 8；该项现已完成，当前进入工作日 9。

### 工作日 7：项目级接口资产编辑器（已完成）

- [x] SQLite 与 MySQL 新增项目级接口模块、运行环境、变量、接口资产和不可变版本表；接口、模块、环境和变量引用均校验当前项目边界，不允许跨项目读取或绑定。
- [x] 接口每次新建或编辑自动产生新版本，发布时归档旧发布版本；手工编辑和 JSON/YAML/OpenAPI/cURL 导入均保留草稿、发布与版本历史，并继续同步现有运行时 path 热更新链路。
- [x] 环境集中管理 Base URL；变量支持项目通用或指定环境作用域，请求定义使用 `{{VARIABLE_NAME}}` 引用。密钥变量使用平台主密钥加密，公开接口只返回“是否已保存”，不返回明文或密文。
- [x] 接口中心保留“接口资产 / 环境与变量 / 批量导入”三个工作区；接口资产页已取消四张首屏统计卡，改为左侧项目选择与项目根节点、全部接口、未分组接口、业务模块树，右侧面包屑、结果数量、搜索、环境筛选和接口清单。模块新建入口固定可见，空项目提供明确引导，新建接口自动继承当前业务模块。
- [x] 明确“烈马”只代表自动化测试平台，不作为业务项目默认值：首次初始化不再预填 `LIEMA / 烈马测试项目`，必须录入真实业务项目；接口中心统一使用“业务项目”称谓，项目管理员可直接编辑当前项目名称与说明，平台管理员通过“用户与项目”创建项目和管理成员。
- [x] 所有项目成员可查看当前项目接口资产，只有项目管理员和平台管理员可修改；模块、环境、变量、接口的创建、编辑、发布和删除均写入不含密钥值的审计记录。
- [x] 静态资源版本提升到 `20260818.5`；真实浏览器完成 1920×1080、1366×768 与 1280×720 接口目录工作区验收，多模块、接口表、状态与操作列均可在首屏管理，1280 宽度无需横向拖动；1366×768 复核平台品牌、业务项目选择与项目编辑框概念分离清晰；此前模块、环境、密钥变量、接口 v1→v2、发布和版本历史交互验收继续有效，页面未回显虚拟密钥值。
- [x] Node JavaScript 语法、Python 编译、定向接口/RBAC/静态页面测试及 PyCharm 虚拟环境全量 **127 项测试通过**。

当时默认下一步为工作日 8；该项现已完成，当前进入工作日 9。

### 服务器 CPU 温度口径与来源修复（已完成）

- [x] 修复 `sensors` 文本解析把 `(high=88°C, crit=98°C)` 阈值当成当前温度的问题；双路 CPU 只比较 `Package id` 当前值，缺少 Package 时才依次回退 Tctl、Tdie 和 Core。
- [x] 未安装 `lm-sensors` 时不再盲取前三个 thermal zone；改为同时读取 sysfs 热区名称与温度，只接受 `x86_pkg_temp`、`cpu-thermal`、`coretemp`、`k10temp` 等可识别 CPU 热区，其他热区显示不可用。
- [x] 实时监控顶部温度卡改为 CPU 专用，不再把 CPU、GPU 平均温度和逐卡温度混合取最大值；按 `Package id 0`、`Package id 1` 或实际 Tctl/Tdie/Core/热区逐项展示当前值、峰值与芯片名称，不再使用笼统的“最热封装/核心”口径。
- [x] 统一实时采集与旧 CPU 压测安全保护的解析器；非登录 SSH 环境同时探测 PATH、`/usr/bin/sensors` 和 `/usr/sbin/sensors`，CSV、摘要及 DOCX/PDF 保留 CPU 温度来源；静态资源版本提升到 `20260818.7`。
- [x] 使用用户提供的双路 coretemp 输出新增回归，确认结果为 Package 0 `36.0°C`、Package 1 `33.0°C`，不采纳 88/98°C 阈值或 48°C 网卡温度；修复前无来源的旧会话明确提示重启服务并重新连接，且不参与页面峰值或重新生成的报告；Node 语法、Python 编译、定向 47 项及 PyCharm 环境全量 **136 项测试通过**。

### 发起测试表单必填规则与条件交互收敛（已完成）

- [x] 在任务配置顶部明确列出必填范围，并为登录用户名、登录密码、业务服务器、案件名称、翻译语言和测试数据增加红色 `*`；独立 GPU 地址与后台路径使用条件必填规则。
- [x] “后台本机上传路径”默认隐藏，仅在高级参数选择“服务器直传”后显示、设为必填并解释其为当前 Python 服务所在电脑可访问的绝对路径；切回网页上传后再次隐藏且不写入任务参数。
- [x] 案件名称按本机当天日期自动生成 `测试YYYYMMDD`，保留编辑能力；重新配置旧任务时优先恢复原案件名称，缺失时补当天默认值。
- [x] “高级运行参数”改为整行可点击的折叠控件，补充设置范围说明、展开/收起状态文字和方向箭头；普通网页上传未选择文件、独立 GPU 未填写地址时在上传前直接阻止提交。
- [x] 静态资源版本提升到 `20260817.8`；浏览器验证默认态、展开态、服务器直传和独立 GPU 条件切换，1280×720 无横向溢出；Node JavaScript 语法检查及 PyCharm 虚拟环境全量 **123 项测试通过**。

当时默认下一步为工作日 7；工作日 7、8 现均已完成，当前进入工作日 9。

### 内网业务 API 被本机代理截获导致登录超时（已修复）

- [x] 根据真实失败日志确认，请求尚未进入业务账号校验：目标为内网业务服务器，但 `requests` 继承了 PyCharm 进程代理，将登录请求转发到本机 `127.0.0.1:7890`，等待 60 秒后超时。
- [x] 目标登录端口直接 TCP 可达；关闭代理继承后，无凭据 HTTP 直连立即返回服务端 404，证明网络链路正常且不再经过本机代理。
- [x] `TargetPlatformClient` 默认设置 `trust_env=False`，内网业务 API 始终直连；仅在部署目标明确要求代理时，通过 `api.use_system_proxy: true` 显式开启。
- [x] 示例和本机 YAML 均加入非敏感代理策略开关，README 同步说明；新增默认直连和显式代理两项回归，使用 PyCharm 虚拟环境完成 Python 编译及隔离全量 **122 项测试通过**。

### GPU 实机复验与业务任务认证安全迁移（已完成）

- [x] 使用用户明确指定的独立 GPU 服务器完成只读实机复验：CentOS 7、4×RTX 4090、驱动 580.65.06；连续 3 个独立采样点均解析为 `available`，显存容量/占比、功耗/上限、SM 频率、风扇和 P-State 全部可采集。
- [x] 实机当时显存占用约 85.4 / 98.3 GiB，按繁忙保护只做在线监控，不启动 `gpu-burn` 或其他负载；目标已保存为平台服务器资产，SSH 凭据使用主密钥加密，不写回 YAML。
- [x] 发起测试改为页面直接提交业务账号密码；业务服务器和独立 GPU 服务器可选择平台资产或手动填写，默认 GPU 与业务服务器同机，只有明确选择不同资产/地址时才拆分。
- [x] 业务账号密码、一次性 SSH 密码和可选业务管理员授权从普通任务参数中剥离，与任务同事务加密保存；任务领取时原子读取并删除，公开列表、详情、日志和历史记录均不可见。
- [x] 历史任务不再直接沿用旧凭据重跑，统一进入“重新配置”并只回填非敏感字段；业务账号登录失败时，只有本次任务同时提供管理员授权才允许尝试自动创建。
- [x] `config.example.yml` 和 `config.local.yml` 删除 `servers`、`test_accounts` 与业务管理员凭据段；配置文件只保留环境连接、非敏感默认值和功能开关。
- [x] 发起测试页面增加服务器资产、独立 GPU 选择和清晰的一次性凭据提示；1920×1080 实际浏览器验证三列基线一致、独立 GPU 字段切换正常、无横向溢出和控制台错误。
- [x] 新增任务密文公开隔离、重启后领取、领取销毁、取消清理、执行时内存注入及服务器资产凭据解析回归；Python 编译、Node JavaScript 语法和隔离全量 **120 项测试通过**。

### YAML 配置职责梳理与废弃项清理（已完成）

- [x] 明确 `config.example.yml` 是可提交的脱敏结构模板，`config.local.yml` 是被 Git 忽略且运行时优先加载的本机真实配置；local 不存在时才回退 example。
- [x] 逐项核对配置加载器及业务调用，确认 SQLite/MySQL、MinIO、监控、压测、导出和 AI 检查仍有实际用途或条件用途；业务账号和服务器目标已迁移到页面与平台资产。
- [x] 删除已迁移到页面/平台存储后的 `servers`、`test_accounts`、业务管理员凭据、废弃 `llm.managed_by` 占位及无调用的 `llm_cfg()`。
- [x] 示例模板补齐 MySQL `read_timeout`、`write_timeout`，并修正已经过时的 SQLite/MySQL 说明，使模板与当前加载逻辑一致。
- [x] 两份 YAML 解析与废弃引用扫描通过；使用临时 SQLite/本地产物目录隔离真实 MySQL、MinIO 后，全量 **120 项测试通过**。

### 登录门户双栏视觉重构（已完成）

- [x] 根据用户实屏标注撤销信息堆叠方案：删除四张能力卡、底部质量流程、右上双标签和底部安全说明，不再使用带外边距、圆角和阴影的大幅悬浮容器。
- [x] 参考企业设备登录页的结构重新建立铺满窗口的 58/42 分屏：左侧深色品牌与一句价值说明，右侧浅色登录面；品牌标识由 43px 提升到 60px，主标题由最高 58px 收敛到最高 48px。
- [x] 右侧标题、说明、用户名、密码和登录按钮统一为 460px 内容宽度和同一左边线；移除输入框前置图标与按钮箭头，减少视觉噪音，会话失效等认证提示继续使用独立提示态。
- [x] 保留首次管理员初始化表单与原认证逻辑；980px 以下切换为上下结构，760px 以下只保留紧凑品牌和双行标题。
- [x] 使用无账号、无数据库的静态隔离预览和非 8080 端口，在 1920×1080、1366×768、390×844 浏览器完成验收；桌面与移动端标题、字段和按钮左边线误差均为 0，无横向溢出或控制台错误。
- [x] 根据最终标注将登录标题收敛为“登录”，删除左侧 `ALL-IN-ONE QUALITY ENGINEERING` 眉题并默认隐藏说明文字；仅在会话失效、退出或连接异常时显示必要提示。
- [x] 静态资源版本提升到 `20260817.7`；1920×1080 实际浏览器复核无横向溢出和控制台错误，Node JavaScript 语法检查及全量 **120 项测试通过**。

当时默认下一步为工作日 7；该项现已完成。GPU 满载验收仍需维护窗口和明确释放的目标卡。

### 工作日 6：用户登录、用户管理与项目级 RBAC MVP（已完成）

- [x] 新增用户、项目、项目成员关系、登录会话和审计事件持久化，SQLite 与 MySQL 使用相同仓储契约；平台不生成默认账号或默认明文密码，首次打开由用户创建首位平台管理员和初始项目。
- [x] 密码使用带随机盐的 scrypt 哈希；登录使用仅保存令牌哈希的 12 小时服务端会话和 HttpOnly、SameSite Cookie；受控局域网 HTTP 与 HTTPS 分别通过 `LIEMA_SESSION_COOKIE_SECURE=false/true` 显式选择 Secure 属性，停用用户会立即撤销其全部会话。
- [x] 所有业务 API、接口文档和 OpenAPI 已接入统一登录门禁；写请求额外校验 CSRF 令牌。`/health`、静态页面和初始化/登录接口保持匿名可达，便于部署探针和首次初始化。
- [x] 建立固定角色矩阵：平台管理员、项目管理员、测试执行者、只读成员；保护最后一位平台管理员不被停用或降级，普通用户不能切换到未授权项目；平台级模型、模板和服务器资产配置仅平台管理员可修改，项目级接口资产由项目管理员维护、项目成员只读。
- [x] 新建业务自动化任务、企业报告和服务器压测任务记录项目与创建人；列表、详情、指标和下载按当前 `X-Project-ID` 隔离。历史无项目标记的数据仅平台管理员可见。
- [x] 新增正式登录/首次初始化界面、顶栏项目切换器与用户身份区，以及用户创建、项目创建、成员角色、账号状态、角色矩阵和审计记录工作区；只读角色不展示发起测试和平台配置入口。
- [x] 使用隔离临时实例在 1400×900 和 1920×1080 浏览器完成初始化、登录、工作台、用户权限页及双项目切换验收；页面无横向溢出，控制台无错误，测试数据未进入正式平台库。
- [x] 静态资源版本提升到 `20260817.2`；Node JavaScript 语法、Python 编译及全量 **115 项测试通过**。

当时默认下一步为工作日 7；该项现已完成。GPU 增强字段实机复验也已完成，不阻塞工作日 8。

### 压测启动后的实时任务监控闭环（已完成）

- [x] 根因确认：提交成功后虽然会切换到“实时监控”，但页面没有压测任务状态、进度和停止入口；2.5 秒轮询也只刷新 SSH 会话与任务列表，没有持续读取当前压测任务及其指标。
- [x] 实时监控首屏新增紧凑的压测任务监控条，统一展示任务名称、执行状态、方案、目标服务器、运行时间、执行进度、后端消息和“取消任务 / 安全停止”操作。
- [x] 启动压测后立即切换并呈现新任务；排队/运行中的任务持续读取任务专属指标，任务结束后保留最终状态，同时恢复展示当前 SSH 会话的最新服务器指标，不用历史任务数据覆盖实时状态。
- [x] 使用真实内网244只读会话完成页面验收，未启动新的压测：任务监控条完整显示，连续轮询后会话采样从 21 次增至 27 次且 CPU/GPU 数值持续变化；验收后关闭会话，活动会话恢复为 0。
- [x] 静态资源版本提升到 `20260815.31`；Node JavaScript 语法检查及全量 **104 项测试通过**。

### 压测配置页直接切换活动会话（已完成）

- [x] 将页头只读的“当前会话”标记替换为正式的“压测会话”选择器，直接列出当前所有活动 SSH 会话，无需返回服务器会话或实时监控页再切换。
- [x] 压测页与实时监控页的会话选择保持同步；切换后仍停留在压测配置页并立即刷新服务器能力、资源摘要和逐卡 GPU 信息。
- [x] 切换服务器时主动清空上一台服务器的能力缓存与 GPU 选择，避免把旧服务器的显卡选择误用于新会话；无活动会话时明确显示“暂无活动会话”。
- [x] 使用两个真实 SSH 会话完成 1400×900 浏览器验收：从内网244（120 线程、3×RTX PRO 5000 72GB Blackwell）切换到内网91（48 线程、4×RTX 4090），页面保持在压测配置且资源正确更新；验收后两个测试会话均已关闭，活动会话恢复为 0。
- [x] 静态资源版本提升到 `20260815.29`；Node JavaScript 语法检查及全量 **103 项测试通过**。

### 实时监控会话工具栏对齐修复（已完成）

- [x] 根据用户实屏反馈读取实际计算尺寸，确认会话下拉框为 `46px` 高且顶部位于 `y=218`，原连接状态仅 `33.2px` 高，操作组又按包含标签的整个字段居中到 `y=204.5`，造成明显错位。
- [x] 工具栏改为底边对齐；“SSH 已连接”和“关闭连接”统一为 `112×46px`、14px 单行文字，与 46px 会话下拉框使用同一控制高度和视觉基线。
- [x] 使用真实“内网91”会话在 1400×900 浏览器窗口完成更严格的窄桌面验收：下拉、状态、按钮完整显示且同底边，状态文字无换行；1920×1080 标准 1K 屏保留更充足横向空间。
- [x] 静态资源版本提升到 `20260815.28`；Node JavaScript 语法检查及全量 **102 项测试通过**。

### 压测配置资源步骤自动展开修复（已完成）

- [x] 根因确认：长连接会话每 2.5 秒心跳都会重新渲染能力数据，旧渲染函数无条件执行 `open = true`，因此用户手动收起“确认服务器资源”后会在下一轮采样时再次展开。
- [x] 初始展开改为进入“压测配置”时仅执行一次；能力数据渲染、实时轮询和方案切换只更新内容，不再修改用户的展开/收起状态。
- [x] 使用真实“内网91”SSH 会话完成 1920×1080 验收：收起后等待 7 秒，采样数从 165 增至 169 仍保持收起；切换“标准测试”以及“实时监控 → 压测配置”往返后状态继续保持。
- [x] 静态资源版本提升到 `20260815.25`；Node JavaScript 语法检查及全量 **101 项测试通过**。

### GPU 单卡详情改为右侧抽屉（已完成）

- [x] GPU 卡片不再在原网格内横向扩展；点击“指标详情”后由内容区右侧滑出固定详情抽屉，多卡总览的列数、位置和对比关系保持不变。
- [x] 右侧抽屉集中展示所选 GPU 的型号、采集状态、最近更新时间、采样次数、使用率/显存/温度当前-平均-峰值、显存容量、功耗上限、SM 频率、风扇、P-State 和两条趋势曲线。
- [x] 当前卡片使用蓝色边界标明选中状态；可直接切换另一张 GPU，抽屉内容原位更新；实时轮询期间抽屉和所选 GPU 均保持，不再发生网格重排。
- [x] 抽屉支持右上角关闭和 Escape 关闭，离开实时监控页签时自动关闭；桌面宽度固定为 620px，窄屏自适应为完整可用宽度。
- [x] 使用真实 3×RTX PRO 5000 72GB Blackwell 会话完成 1920×1080 验收：抽屉位于 `x=1300–1920`、内部无横向溢出，GPU 0/1 切换、连续采样保持和关闭均正常。
- [x] 静态资源版本提升到 `20260815.24`；Node JavaScript 语法检查及全量 **100 项测试通过**。

### GPU 指标详情入口去按钮化（已完成）

- [x] 根据用户实屏反馈移除每张 GPU 卡片底部的整行描边按钮，不再使用居中文字和字符箭头占据完整卡片宽度。
- [x] 入口改为右下角紧凑的“指标详情 + Chevron”轻操作，无底色、无分割线；仅保留文字颜色、键盘焦点和轻微悬停反馈。
- [x] 展开后文案切换为“收起详情”，Chevron 同步旋转，按钮提供对应 GPU 编号的动态无障碍标签。
- [x] 使用真实 8×RTX 3090 会话完成 1920×1080 验收：8 个入口宽度均为 72px，四列布局无横向溢出；GPU 0 展开后详细指标正常显示，状态保持正确。
- [x] 静态资源版本提升到 `20260815.23`；Node JavaScript 语法检查及全量 **100 项测试通过**。

### 运行总览去 Demo 化与最近执行记录重构（已完成）

- [x] 完整删除“上线准备度”面板、DOM 标识、评分计算以及模型/模板保存后的无效联动，不再保留仅供展示的 Demo 式配置清单。
- [x] “当前任务”改为满宽业务区域；无活动任务时使用紧凑横向空状态，将更多首屏空间留给真实历史数据，活动任务的阶段、进度和实时监控入口保持不变。
- [x] “最近运行”升级为“最近执行记录”，直接展示真实任务中的测试案件、业务服务器、测试账号、测试内容、翻译语种、开始时间、耗时、最终状态和结果说明；删除对已完成任务无信息增量的“阶段 / 100% 进度”重复列。
- [x] 历史表格仅在任务内容实际变化时更新，避免 2.5 秒轮询无条件重建 DOM 导致“查看 / 重跑”按钮点击瞬间失效。
- [x] 使用当前真实历史任务完成 1920×1080 浏览器验收：当前任务和历史区域均占满内容宽度，7 列无横向溢出，首屏可见 5 条执行记录；等待多轮轮询后“查看”可正常进入实时监控。
- [x] 静态资源版本提升到 `20260815.22`；Node JavaScript 语法检查及全量 **100 项测试通过**。

### 参考服务器监控中心的简约客观风格与统一深色修正（已完成）

- [x] 提炼用户参考图真正需要复用的规则：扁平卡片、状态色细边框与小圆点、三项即时指标同层展示，避免渐变、发光和多层小卡套娃；不照搬参考图的白色主题。
- [x] 撤销实时监控页签的独立浅色画布和页面状态主题切换；服务器会话、实时监控、压测配置和任务记录现在始终使用同一套深色背景，不再出现黑白跳变。
- [x] GPU 总览使用四张带线性图标的扁平摘要卡；逐卡默认只展示使用率、显存占用和温度，所有曲线默认隐藏，点击“查看完整指标”后卡片整行展开详细指标与两张趋势图。
- [x] 使用真实长连接的 8×RTX 3090 会话完成 1920×1080 验收：会话页与监控页背景均为统一深色，四列卡片无横向溢出；等待多轮轮询后展开状态保持，控制台无错误。
- [x] 该阶段静态资源版本提升到 `20260815.21`；当前已随压测实时任务监控升级到 `20260815.31`，Node JavaScript 语法检查及全量 **104 项测试通过**。

### GPU 多卡监控渐进式卡片重构（已完成）

- [x] 根据真实 1920×1080、8×RTX 3090 页面截图复盘：原卡片把七项指标和两张曲线同时压入四列网格，信息层级不清、字号被迫缩小，已改为“概要优先、详情展开”的渐进式结构。
- [x] 每张 GPU 卡默认只展示使用率、显存占用、温度三项核心状态及当前/平均/峰值；完整显存容量、功耗/上限、SM 频率、风扇和 P-State 收入详情区，点击后卡片横跨两列展示。
- [x] 展开/收起状态保存在前端监控状态中，2.5 秒轮询重绘后仍保持；点击详情按钮不会误触压测配置中的 GPU 选择逻辑。
- [x] 删除“有变化/读数稳定”等难以解释的技术判断，曲线统一标注“最近 N 次采样”；数值变化较小时明确说明采集仍在持续，不再把平直曲线包装成含混状态。
- [x] 该阶段静态资源版本提升到 `20260815.18`；真实长连接会话完成四列布局、详情展开和多轮采样保持验证，浏览器控制台无错误，Node JavaScript 语法检查及全量 **99 项测试通过**；随后升级为 `20260815.21` 的扁平统一深色监控页。

### 用户截图集中反馈的 5 项缺陷修复（已完成）

- [x] 修复服务器抽屉内复选框被普通输入框 `43px` 最小高度覆盖的问题；密码保存选项实测恢复为单一 `18×18px` 控件，不再出现双层方框。
- [x] 修复环境检测在无空闲 GPU 时强制切换“自定义”方案的问题；当前预设继续保持，平台仅按安全策略取消不可执行的 GPU 负载，用户点击快速、标准、稳定性方案均可正常保持选中。
- [x] 8 卡实时监控在 1600px 以上宽屏使用 4 列 × 2 行布局；本阶段曾完成紧凑卡片，后续已按用户实屏反馈升级为概要卡片与两列宽度的可展开详情。
- [x] GPU 采样提示增加采样次数和最近更新时间；本阶段曾显示“有变化/读数稳定”，后续已统一替换为更清楚的“最近 N 次采样”。
- [x] 关闭 SSH 会话立即切换为“正在断开/正在关闭”并阻止轮询覆盖，后端清理移入线程池；模型连通测试增加按钮忙碌态和卡片内成功/失败结果，模型请求同样移入线程池，避免页面无响应。
- [x] 静态资源版本提升到 `20260815.16`；Node JavaScript 语法检查通过，1280×720 隔离浏览器完成复选框、预设切换和模型失败反馈验收，全量 **98 项测试通过**。

### 服务器资产控制台视觉与交互重构（已完成）

- [x] 根据用户 1K 屏实际截图确认：原“左侧空列表 + 右侧常驻表单”造成主次割裂、空框面积过大且操作路径不符合成熟基础设施产品习惯，已停止沿用该布局。
- [x] 服务器会话首页改为全宽资产控制台：上方展示服务器总数、活动会话、加密凭据和每次授权四项概览，下方使用服务器表格集中呈现地址、SSH 账户、凭据策略、会话状态、最近使用与操作。
- [x] 新增名称/地址/用户搜索和会话状态筛选；无资产时使用紧凑空状态，存在资产时按行操作连接、监控、编辑和删除，不再用大卡片堆叠。
- [x] 添加、编辑和未保存密码时的补录改为右侧抽屉；基础信息、SSH、保存策略和安全说明分区展示，操作按钮固定在抽屉底部，补录密码时自动聚焦密码框。
- [x] 静态资源版本提升到 `20260815.15`；在 1280×720 隔离浏览器中完成空状态、两种凭据策略、两行资产、搜索筛选、抽屉滚动及未保存密码焦点验收；JavaScript 语法、页面选择器完整性、Python 编译及全量 **97 项测试通过**。

### 服务器资产与长连接会话管理重构（已完成）

- [x] 服务器性能入口调整为“服务器会话 → 实时监控 → 压测配置 → 任务记录”：实时状态是主能力，压测在已连接会话内按需启动，不再要求每次重复填写服务器。
- [x] 新增 SQLite/MySQL 服务器资产表和列表管理；服务器配置与 SSH 密码分别决定是否保存，密码仅使用平台主密钥加密，公开接口永不返回密码或密文；未保存密码的服务器下次连接会明确要求重新输入。
- [x] 新增进程内长连接会话管理器：建立 SSH 后持续运行性能采集，页面活动时发送心跳；用户显式关闭立即释放，离开页面后默认空闲 5 分钟自动回收；执行压测期间禁止误关会话。
- [x] 压测任务复用当前会话的 SSH 与监控器，不再复制或持久化会话密码；能力探测失败时保持已建立的 SSH 和实时监控，仅将不安全的 CPU/GPU 压测能力降级阻止。
- [x] GPU 选择卡的长型号取消截断并自动换行；任务记录增加报告选择器和每行 DOCX/PDF 下载入口，不再固定下载第一份报告。
- [x] 静态资源版本提升到 `20260815.14`；在 1280×720 隔离浏览器中完成未保存密码提示、长连接/心跳、降级监控、会话内压测限制、显式关闭和两份报告选择验收；Python 编译、JavaScript 语法检查及全量 **97 项测试通过**。

### 在线体检被旧执行器抢占与任务恢复入口修复（已完成）

- [x] 根据用户任务记录和平台数据库只读核验确认：最新“在线体检”任务在提交后约 0.15 秒即被旧逻辑中断，但对应加密凭据仍在安全表中，说明不是密码错误，而是共享同一 MySQL 的旧执行器抢先领取了新任务。
- [x] 新提交的性能任务改为 `authorized` 授权态；当前执行器兼容领取授权态和历史 `queued` 任务，旧版仅查询 `queued` 的执行器无法再抢走新任务。
- [x] 凭据恢复、启动清理、缺失凭据恢复和取消逻辑同步支持授权态；新增回归测试证明新任务对旧领取查询不可见、对当前执行器可领取。
- [x] 任务记录操作列改为固定可见，恢复入口更名为“重新配置授权”；点击后恢复非敏感配置、清空密码、重置环境检测并将焦点定位到 SSH 密码框。
- [x] 在 1280×720 隔离浏览器中复核：任务表无横向滚动，操作按钮完整可见，恢复流程字段和焦点正确；临时实例与测试数据已关闭清理。
- [x] 静态资源版本提升到 `20260815.13`；Python 编译、JavaScript 语法检查及全量 **92 项测试通过**。

### 临时页面 Key 认证移除（已完成）

- [x] 删除首页 Key 输入遮罩、连接按钮、重新输入 Key 入口和浏览器会话存储，页面打开后自动加载工作台。
- [x] 删除前端所有 `X-API-Key` 请求头以及报告、压测报告下载时的独立 Key 传递；轮询和服务失败状态改为普通连接状态处理。
- [x] 后端两个 `/api` 路由组取消 Key 依赖，生产环境不再要求 `LIEMA_WEB_API_KEY`；`LIEMA_MASTER_KEY` 继续用于模型 Key 和排队 SSH 凭据加密，不受本次改动影响。
- [x] 修复根目录兼容启动器 `web_main.py` 仍引用已删除 `_LOCAL_DEV_API_KEY` 的启动回归；直接运行与 `uvicorn web_main:app` 均统一转发到新版入口，不再保留重复的旧启动提示。
- [x] 启动说明和 README 已同步调整，并明确正式用户管理上线前仅用于本机或受控内网，不应直接暴露公网。
- [x] 隔离临时实例真实浏览器验收：无需填写 Key 即进入运行总览，页面显示“连接正常”，bootstrap、任务、报告、模型、接口和压测接口均返回 200；临时实例与测试数据已关闭清理。
- [x] 该阶段静态资源版本提升到 `20260815.12`；用户实际虚拟环境完成启动入口导入验证，Python 编译、JavaScript 语法检查及当时全量 **90 项测试通过**。

### 全站高优先级浅色规则清理（已完成）

- [x] 根据“发起测试 → 高级运行参数”实际截图定位根因：旧规则 `.advanced-options .choice-box` 选择器优先级高于新版通用深色规则，导致四组参数卡片重新显示大面积白底。
- [x] 为高级参数卡片、图例和选项建立 `#view-new-run` 主题边界；同时完成同类规则审计，覆盖服务器直传上传区、GPU 空状态与设备卡选中/繁忙/阻断/降级状态，以及错误提示。
- [x] 真实浏览器展开高级参数并逐项读取计算样式：4 组卡片背景均为 `rgb(10,24,37)`，选项背景为 `rgb(16,34,52)`；服务器直传上传区使用深色渐变，未再出现白底。
- [x] 静态资源版本提升到 `20260815.11`；新增高优先级状态组件回归测试，JavaScript 语法检查及全量 **87 项测试通过**。

### 服务器性能页深浅色混用修复（已完成）

- [x] 根据用户 1920×1080 实际截图确认：服务器配置步骤卡和环境检测结论被旧浅色规则覆盖，形成大面积白底、浅色文字，属于不可交付的对比度缺陷。
- [x] 为 `#view-stress` 工作区增加独立主题边界，展开步骤、折叠步骤、内容区、字段标签和辅助说明统一锁定深色样式。
- [x] 环境检测的等待、正常、注意、阻断和错误状态全部使用深色状态卡，不再出现白底浅字；不同状态继续保留蓝、黄、红边界语义。
- [x] 浏览器读取实际计算样式确认：展开卡背景 `rgb(12,29,44)`、检测结论背景 `rgb(10,25,38)`、主要文字 `rgb(198,213,227)`，不存在白色背景。
- [x] 静态资源版本提升到 `20260815.10`；新增主题隔离回归测试，JavaScript 语法检查及全量 **86 项测试通过**。

### 报告中心半屏留白修复（已完成）

- [x] 根因确认：旧样式仍在外层报告网格保留 `1040/1180px` 限宽，模板面板只占第一列，导致 1920×1080 屏幕右侧近半内容区空白。
- [x] 从旧样式源头删除报告生成和模板管理的固定限宽，生成报告、模板管理、报告任务三个页签统一占满可用内容区。
- [x] 六章模板改为按真实内容宽度自适应：章节卡最小 430px，1920×1080 下三列两行，1280 宽验收窗口下两列，窄屏单列。
- [x] 章节卡统一等高，编辑区随卡片伸展，标题、模式、启用状态和固定表达保持完整对齐；静态资源版本提升到 `20260815.9`。
- [x] 实际浏览器逐一复核生成报告、模板管理、报告任务三个页签，均无右侧留白和覆盖；JavaScript 语法检查及全量 **85 项测试通过**。

### 模型配置安全提示企业化重构（已完成）

- [x] 将右侧狭小的“Key 不进入 yml”提示卡改为正式的“API Key 安全保护”面板，使用 52px 盾牌图标、22px 标题、14–15px 正文与清晰的安全状态。
- [x] 按“安全提交 / 密文存储 / 按需解密”展示三段处理边界，明确密钥不进入 yml、任务日志和列表响应，编辑时留空保留原 Key。
- [x] 将模型列表独立为“已配置模型”面板，新增动态数量、可读空状态以及后续编辑、连通测试、启用操作说明。
- [x] 模型配置区在桌面端为安全面板预留至少 360px 宽度，并在窄屏切换为自适应双列/单列；静态资源使用独立版本参数强制刷新。
- [x] 在 1280×720 实际浏览器中完成空模型状态视觉复核，安全标题、流程、状态及表单均无覆盖；JavaScript 语法检查及全量 **84 项测试通过**。

### 标准 1K 屏幕字号与旧样式缓存修复（已完成）

- [x] 确认用户截图仍加载旧轻色紧凑样式：静态资源版本停留在 `20260815.6`，旧层包含 8.5–11px 辅助文字、表格和页签字号，并非显示器缩放问题。
- [x] 静态资源持续使用版本参数刷新（当前为 `20260815.31`），确保普通刷新即可加载最新企业样式，不再被浏览器旧缓存覆盖。
- [x] 建立 1920×1080、100% 缩放桌面可读性基线：正文 16px、页面标题 25px、侧栏/表格 15px、标签/页签 14px、说明文字 13px，表单控件最低 46px，品牌 Logo 48px。
- [x] 修复放大后发现的性能配置展开区、环境检测结论卡片深浅色冲突及页签纵向滚动条；最近任务名称、说明和时间同步提升字号。
- [x] 在更窄的 1280×720 浏览器窗口复核服务器性能配置页和任务记录页，无遮挡且文字、状态、时间清晰可辨；JavaScript 语法检查及全量 **83 项测试通过**。

### 业务任务监控口径调整：GPU 只展示均值与峰值（已完成）

- [x] 业务自动化“实时监控”中的 GPU 统计卡由“当前 / 峰值”改为“均值 / 峰值”，均值按当前任务已加载的有效 GPU 采样计算。
- [x] GPU 趋势曲线继续保留，用于查看任务执行期间的负载变化；CPU 和内存卡片维持原有口径。
- [x] “服务器性能”模块的 GPU 当前/平均/峰值、逐卡卡片和曲线均未修改，继续承担瞬时状态查看职责。
- [x] 使用真实历史任务浏览器复核：页面显示 GPU 均值/峰值 `44.8% / 73.7%`，均值与任务报告记录一致；全量 **82 项测试通过**。

### 客户可用性缺陷修复：任务未传默认翻译语种（已完成）

- [x] 核对 11:35 失败日志对应的持久化任务，确认 Web 提交参数缺少 `translate_name`，蓝鲨返回“未选择语种”，不是状态解析误判。
- [x] 增加平台级 `defaults.translate_name`，当前默认值为 `es:zh-CHS`；账号模板已在后续认证安全迁移中移除，当前只使用页面输入或该平台默认值。
- [x] 页面首次进入时自动显示平台默认语种，仍允许用户按本次任务修改；账号、服务器、路径和案件字段继续保持空白，或由用户主动选择服务器资产后带入目标环境。
- [x] `/api/run` 在任务入队前补齐默认语种；如果平台和页面均无语种则直接返回 400，不再让任务运行到翻译轮询阶段才超时。
- [x] 执行器增加最终回退，兼容旧页面和 API 调用；历史任务需重新配置凭据后提交。实际浏览器确认默认语种展示为 `es:zh-CHS`。

### 客户可用性缺陷修复：发起测试不再自动填入默认业务数据（已完成，后续已升级）

- [x] 发起测试页面不再自动选择任何 YAML 账号；后续已完全删除账号模板，业务账号必须在本次任务页面直接填写。
- [x] 首次进入时登录用户名、业务服务器、独立 GPU 服务器、SSH 用户、上传路径和案件名称均保持空白；翻译语言使用平台默认值，不再从环境配置回填业务服务器或 `root`。
- [x] 取消自动生成当天案件名称；只有用户主动选择服务器资产时才带入非敏感环境字段，手动模式保持可编辑。
- [x] 服务器资产切回手动模式会清空资产带入字段和密码框，避免误提交上一服务器配置。
- [x] 新增业务账号、业务服务器必填和服务器资产切换回归；实际浏览器验证初始空白、资产带入及独立 GPU 切换均符合预期。

### 客户可用性缺陷修复：排队任务重启后凭据丢失（已完成）

- [x] 根因确认：原调度器只把 SSH 密码保存在进程内存，恢复逻辑会将所有需要密码的排队任务直接标记为“已中断”。
- [x] 新增独立加密凭据表；任务提交时使用 `LIEMA_MASTER_KEY` 加密并与任务同事务保存，密码不进入任务参数、列表/详情接口、日志或历史记录。
- [x] 调度器领取任务时原子读取并删除密文，解密后的密码只存在于任务运行内存；领取失败、取消、结束及服务恢复时清理无效密文。
- [x] 服务重启后，有完整加密凭据的排队任务保持排队并继续调度；运行中任务仍按安全边界标记中断，不尝试跨进程恢复 SSH 会话。
- [x] 任务记录中的失败/中断项增加“重新授权/重新提交”，自动带入原任务非敏感配置，用户只需补充密码并重新检测。
- [x] 新增加密往返、公开接口隔离、重启恢复、领取销毁、取消清理和重启后实际调度测试；JavaScript 语法检查及全量 **78 项测试通过**。

### 优先插入任务：全站前端体验二次重构（已完成）

- [x] 首版轻色紧凑方案虽减少了滚动，但字体、侧栏、表单和空间利用未达到产品视觉标准；已根据实际截图复核结果停止沿用该方向。
- [x] 正式页面改为用户最初参考图的深色企业监控台方向：桌面正文提升至 16px，标题、标签、说明和数据层级同步放大，表单控件统一为最低 46px 高度。
- [x] 品牌 Logo 放大至 48px，删除“质量工程工作台”小字；侧栏按“测试工作台 / 平台能力”重新分组，统一现代线性图标和蓝色选中态。
- [x] 发起测试表单使用固定标签基线和等高控件，三列表单对齐；模型、接口、报告模板等录入区域统一为深色专业表单样式。
- [x] 服务器性能页为页签预留独立 46px 网格行和 18px 行距，实时指标与 GPU 面板按正常文档流分层，实际浏览器验收无覆盖遮挡。
- [x] 报告生成、模板管理和报告任务取消 1040/1180px 限宽，全部使用内容区宽度；报告表单按双列编辑区组织，任务表满宽展示。
- [x] 1280×720 实际浏览器逐页检查 7 个主页面及性能/报告子页，控制台无应用错误；页面结构测试、JavaScript 语法检查及全量 **75 项测试通过**。

### 工作日 5：服务器性能闭环与 GPU 实机增强验收（已完成）

已完成：

- [x] 业务服务器与 GPU 服务器同机时复用 SSH 连接和密码。
- [x] 独立 GPU 服务器改为可选，只有用户明确填写不同地址才启用。
- [x] 修复 GPU 未采集时前端错误显示 `0%`。
- [x] 扩展 `nvidia-smi` 逐卡字段：显存总量/占比、功耗/上限、SM 频率、风扇、P-State；不支持字段保留为 N/A。
- [x] 重做实时 GPU 区域：总体概览、每卡独立卡片与曲线、当前/平均/峰值、明确采集状态，并兼容旧任务样本。
- [x] 服务器性能改为服务器资产与长连接会话优先；保存配置/加密保存密码可独立选择，会话支持显式停止、空闲回收和压测复用。
- [x] GPU 长型号完整换行展示；任务记录可选择任一任务报告并逐行下载 DOCX/PDF。
- [x] DOCX/PDF 同步新增总体与逐卡指标、功耗曲线及采集状态；PDF 逐页渲染验收通过，DOCX 完成结构与内容核验。
- [x] 真实配置服务器连续采集 3 个独立时点，验证无 `nvidia-smi` 时状态、原因及 CSV 持久化均为 `unavailable`；全量 73 项测试通过。
- [x] 用户明确指定的 CentOS 7 / 4×RTX 4090 实机连续 3 个独立时点均为 `available`，增强字段完整；因当时显存占用较高，按安全策略不启动满载压测。

随后工作：工作日 6、7、8 均已完成，当前默认进入工作日 9；GPU 正式满载仅在维护窗口和目标卡明确释放时安排。

## 7. 兼容性验证资源

- [x] openEuler 24.03、x86_64、8×RTX 3090：预检、无压枚举和通用镜像已验证；节点有业务负载时安全阻断烧卡。
- [!] Ubuntu LTS：待提供测试机。
- [x] CentOS 7、x86_64、4×RTX 4090：驱动枚举、在线采样和全部增强字段已验证；高显存占用时安全跳过烧卡。
- [!] RTX 5090、RTX A6000、RTX PRO 6000 Blackwell：待更多型号实机矩阵验证。
- [!] GPU 正式满载：需要维护窗口或明确释放的目标卡。

## 8. 新会话工作规则

1. 先读本文件，再读 `PROJECT_STRUCTURE.md`；只在相关模块需要时再读 `docs/` 对应专题。
2. 修改前核对当前代码，不把本文件中的“计划”误当成已经实现。
3. 每次完成实质工作后更新：最后更新日期、能力进度、十日计划、当前工作和测试数量。
4. 同一时间只保留 1～3 个明确的下一步，避免计划失控。
5. 不在本文件记录服务器密码、数据库名、Access Key、API Key 或其他敏感信息。
6. 新增、删除、移动或重命名目录、源码分层、启动入口或部署文件时，必须在同一次改动中更新 `PROJECT_STRUCTURE.md` 并运行结构回归。

## 9. 按需阅读索引

- 启动与代码结构：`README.md`
- 完整目录职责与接手顺序：`PROJECT_STRUCTURE.md`
- 第 1～4 工作日记录：`docs/day1-architecture-and-server-compatibility.md` 至 `docs/day4-server-compatibility.md`
- GPU 型号与镜像策略：`docs/gpu-compatibility-strategy.md`
- 实机兼容矩阵：`docs/server-compatibility-matrix.md`
- 交付与验收说明：`docs/delivery-acceptance.md`
- 部署、备份与回滚：`docs/deployment-backup-rollback-runbook.md`
- 2026-08-24 灾备与镜像回滚实录：`docs/disaster-recovery-rehearsal-20260824.md`
- 模型评测模块技术方案：`docs/model-evaluation-technical-design.md`
- PyCharm 构建与局域网部署入口：`docs/pycharm-lan-deployment-guide.md`
- 日报格式：`docs/daily-report-template.md`
- 原始需求来源：用户桌面的《自动化测试平台后续开发.md》（本文件已提炼，不必每次重复读取）
